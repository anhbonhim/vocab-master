---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - app/src/main/java/com/nhimz/vocabmaster/ui/VocabMasterApp.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/navigation/Screen.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/MainViewModel.kt
  - app/build.gradle.kts
autonomous: true
requirements: [ARCH-01, UX-01]
must_haves:
  truths:
    - "Screen navigation handles argument passing in a type-safe manner."
  artifacts:
    - app/src/main/java/com/nhimz/vocabmaster/ui/VocabMasterApp.kt
    - app/src/main/java/com/nhimz/vocabmaster/ui/navigation/Screen.kt
  key_links:
    - VocabMasterApp -> Screen (Type-Safe Navigation)
---

<objective>
Migrate application navigation to Compose Navigation 2.8.0 Type-Safe APIs utilizing Kotlin Serialization.

Purpose: Eliminate fragile string-based route parsing and ensure compile-time safety for all navigation arguments across the app.
Output: Refactored VocabMasterApp NavHost and updated Screen definitions using @Serializable.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/data/com.termux/files/home/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Kotlin Serialization and Dependencies</name>
  <files>app/build.gradle.kts</files>
  <read_first>app/build.gradle.kts</read_first>
  <acceptance_criteria>
    - kotlinx-serialization-json dependency is added if missing.
    - Previous navigation dependencies (navigation3) are removed to avoid conflicts.
  </acceptance_criteria>
  <action>
    Update app/build.gradle.kts. Check if `org.jetbrains.kotlinx:kotlinx-serialization-json` is in dependencies; if not, add it. The project is migrating fully to Compose Navigation 2.8.0+, so remove the existing `androidx.navigation3:navigation3-runtime` dependencies to prevent coexistence confusion. Keep the existing `androidx.navigation:navigation-compose` dependency or ensure it's at least version 2.8.0. Do not re-apply the serialization plugin if it's already there. Sync the project.
  </action>
  <verify>
    <automated>./gradlew buildEnvironment</automated>
  </verify>
  <done>Dependencies configured for type-safe navigation and legacy nav dependencies removed.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Routes, NavHost, and MainViewModel</name>
  <files>
    app/src/main/java/com/nhimz/vocabmaster/ui/navigation/Screen.kt
    app/src/main/java/com/nhimz/vocabmaster/ui/VocabMasterApp.kt
    app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/MainViewModel.kt
  </files>
  <read_first>
    app/src/main/java/com/nhimz/vocabmaster/ui/navigation/Screen.kt
    app/src/main/java/com/nhimz/vocabmaster/ui/VocabMasterApp.kt
    app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/MainViewModel.kt
  </read_first>
  <acceptance_criteria>
    - Screen.kt uses @Serializable objects and data classes.
    - VocabMasterApp NavHost uses composable&lt;Route&gt; { }.
    - Arguments are extracted safely via toRoute&lt;T&gt;().
    - MainViewModel is updated to remove the obsolete legacy navigation logic (e.g. _currentScreen).
  </acceptance_criteria>
  <action>
    Convert all entries in `Screen.kt` from a sealed class to top-level `@Serializable` objects (for screens without args, e.g., `object Home`) or data classes (for screens with args, e.g., `data class Quiz(val cardIds: List<String>? = null)`). In `VocabMasterApp.kt`, rewrite the `NavHost` to use the type-safe DSL (`composable<Home> { ... }`). Extract arguments using `backStackEntry.toRoute<T>()`. Update all navigation calls throughout `VocabMasterApp.kt` to use the new type-safe objects instead of strings or the old sealed class instances. 

    Update `MainViewModel.kt`: Remove `_currentScreen` StateFlow and `fun navigateTo(screen: Screen)` as they are no longer needed after switching to Compose Navigation. Update any references to `Screen.Home` or `Screen.Welcome` in the `init` block or `completeOnboarding()` to trigger standard events rather than setting internal ViewModel state.

    Implement standard navigation transitions: Forward (slide-in from right, FastOutSlowInEasing), Back (slide-out to right), Quiz to Result (crossfade 400ms) per UI-SPEC.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
    <human-check>Navigate through the app (Home -> Quiz -> Result -> Home) to verify transitions and argument passing.</human-check>
  </verify>
  <done>App navigation is entirely type-safe and animated, and MainViewModel is cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal | Inter-component routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
| T-03-03 | Tampering | Navigation Payloads | low | accept | Payloads are serialized locally within the app process. |
</threat_model>

<verification>
App compiles, and type-safe routing successfully passes arguments between screens without crashing.
</verification>

<success_criteria>
Legacy string-based navigation is eliminated.
</success_criteria>

<output>
Create .planning/phases/03-compose-ui-refactoring-polish/03-03-SUMMARY.md when done
</output>