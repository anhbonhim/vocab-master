---
phase: "03"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/nhimz/vocabmaster/ui/screens/QuizScreen.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/QuizViewModel.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt
autonomous: true
requirements: [ARCH-01, ARCH-02, UX-02, UX-03, UI-01]
must_haves:
  truths:
    - "Screen Composables are split into stateful Container and stateless Content components."
    - "All unsafe forced unwraps (`!!`) and unsafe `as` casts are eliminated in the presentation code."
    - "Quiz screens survive orientation changes without losing current question session states."
    - "UI components use cohesive typography, padding, color palette, and modern response feedback animations."
    - "If session has 0 questions, navigate back to Home with Snackbar \"Không có câu hỏi nào.\""
    - "Full-screen centered CircularProgressIndicator while questions load from DB"
    - "Normal quiz flow with progress bar, question card, option cards"
    - "Text wraps with maxLines=3, overflow=Ellipsis, card height expands"
    - statement: "Visual UI-state test: question prompts exceeding 2 lines should wrap without clipping"
      verification: backstop
  artifacts:
    - app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt
    - path: app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt
      provides: Duo3DButton
    - path: app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt
      provides: Duo3DOptionCard
    - path: app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt
      provides: DuoProgressBar
    - path: app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt
      provides: DuoFeedbackBanner
    - path: app/src/main/java/com/nhimz/vocabmaster/ui/screens/QuizScreen.kt
      provides: QuizScreenContent
    - path: app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/QuizViewModel.kt
      provides: QuizViewModel
  key_links:
    - QuizScreen -> QuizViewModel (SavedStateHandle)
    - QuizScreen -> DuoComponents
---

<objective>
Refactor the QuizScreen into a stateless component, implement the rich Duo 3D components, and ensure state survival via SavedStateHandle.

Purpose: Deliver the core highly-interactive quiz experience with Duolingo-style 3D aesthetics and animations while preventing state loss on rotation.
Output: Stateless QuizScreenContent, QuizViewModel with SavedStateHandle, and the remaining 3D interactive components.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/data/com.termux/files/home/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Duo 3D Components</name>
  <files>app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt</files>
  <read_first>app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt</read_first>
  <acceptance_criteria>
    - Duo3DButton, Duo3DOptionCard, DuoProgressBar, and DuoFeedbackBanner are created from scratch.
    - Long text in OptionCards wraps to max 3 lines.
    - 3D effects use 4dp bottom shadows.
  </acceptance_criteria>
  <action>
    Add the remaining interactive components to DuoComponents.kt. Implement Duo3DButton (primary CTA with 4dp bottom shadow, translate Y +4dp on press). Implement Duo3DOptionCard (handling default, selected, correct, incorrect states with color borders/backgrounds). Implement DuoProgressBar (horizontal, rounded ends, animated fill). Implement DuoFeedbackBanner (bottom slide-in panel for correct/incorrect feedback). Ensure long text in OptionCards wraps up to 3 lines with Ellipsis overflow per UI-SPEC. Build these completely from scratch per user decision D-01.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
    <human-check>Preview components in Android Studio to verify 3D depth and state changes.</human-check>
  </verify>
  <done>All Duo 3D interactive components are available.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor QuizViewModel for State Survival</name>
  <files>app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/QuizViewModel.kt</files>
  <read_first>app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/QuizViewModel.kt</read_first>
  <acceptance_criteria>
    - Quiz session state survives orientation changes.
    - Unsafe casts are removed.
  </acceptance_criteria>
  <action>
    Refactor QuizViewModel to inject and utilize SavedStateHandle. Save critical properties like the current sessionId/nodeId and currentIndex into the SavedStateHandle so they survive process death and configuration changes (e.g., orientation). Upon recreation, if a sessionId exists in the handle, re-query the questions from the database and jump directly to the saved currentIndex. Remove any unsafe forced unwraps or raw casts in the ViewModel logic.
  </action>
  <verify>
    <automated>./gradlew testDebug --tests \"*QuizViewModelTest*\" || echo \"QuizViewModelTest missing, but tests compile\"</automated>
  </verify>
  <done>QuizViewModel correctly restores state using SavedStateHandle.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor QuizScreen and Add Animations</name>
  <files>app/src/main/java/com/nhimz/vocabmaster/ui/screens/QuizScreen.kt</files>
  <read_first>app/src/main/java/com/nhimz/vocabmaster/ui/screens/QuizScreen.kt</read_first>
  <acceptance_criteria>
    - QuizScreen is split into Container and Content.
    - Uses new Duo 3D components.
    - Animations (shake for incorrect, scale/highlight for correct) are implemented.
  </acceptance_criteria>
  <action>
    Split QuizScreen into Container and Content. Replace legacy UI with the new Duo3DButton, Duo3DOptionCard, DuoProgressBar, and DuoFeedbackBanner. Implement the interaction choreographies: correct answers trigger a green highlight and slide-up banner; incorrect answers trigger a red highlight, a 6dp horizontal shake animation (using Animatable or transition), and the slide-up banner showing the correct answer. Handle the empty state: if the loaded session has 0 questions, navigate back to Home and show a DuoSnackbar "Không có câu hỏi nào." Show a full-screen CircularProgressIndicator while initially loading. Remove all unsafe casts.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
    <human-check>Run the Quiz on a device. Select a correct and incorrect answer to observe the animations and banners. Rotate the screen mid-quiz to verify the question index is preserved.</human-check>
  </verify>
  <done>QuizScreen is stateless, uses modern 3D components, animates feedback, and handles edge cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→UI | User interaction with quiz state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
| T-03-02 | Tampering | Quiz State | low | accept | State is local to the device and resetting it only affects the local user session. |
</threat_model>

<verification>
Quiz flow works end-to-end, animations play correctly on answers, and rotating the device does not reset the question index.
</verification>

<success_criteria>
QuizScreen is fully modernized, safe, and state-resilient.
</success_criteria>

<output>
Create .planning/phases/03-compose-ui-refactoring-polish/03-02-SUMMARY.md when done
</output>