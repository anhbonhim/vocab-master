---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/nhimz/vocabmaster/ui/theme/Theme.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/screens/HomeScreen.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/screens/SettingsScreen.kt
autonomous: true
requirements: [ARCH-01, ARCH-02, UI-01]
must_haves:
  truths:
    - "Screen Composables are split into stateful Container and stateless Content components."
    - "All unsafe forced unwraps (`!!`) and unsafe `as` casts are eliminated in the presentation code."
    - "UI components use cohesive typography, padding, color palette, and modern response feedback animations."
    - "Empty results render \"Chưa có bài học nào\" + onboarding CTA per Copywriting Contract"
    - "Shimmer skeleton placeholders shown while ViewModel loads section data"
    - "UiState.Error with Snackbar \"Không thể tải dữ liệu\" + LocalLogger stack trace"
    - "Snackbar with \"Đã xảy ra lỗi. Vui lòng thử lại sau.\" per D-05"
    - statement: "Visual UI-state test: layout must handle 0 sections (empty state), 1 section, and 20+ sections without breaking scroll"
      verification: backstop
  artifacts:
    - app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt
    - Duo3DCard
    - DuoSnackbar
    - HomeScreenContent
    - SettingsScreenContent
  key_links:
    - HomeScreen -> DuoComponents
---

<objective>
Decouple HomeScreen and SettingsScreen into Container/Content patterns and establish the base 3D Design System theme and components.

Purpose: Isolate UI rendering from state management to improve testability and previewability, while standardizing the visual language per D-01 and D-03.
Output: Stateless screen contents and new Duo3DCard/DuoSnackbar components.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/data/com.termux/files/home/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DuoComponents and Base Theme</name>
  <files>
    app/src/main/java/com/nhimz/vocabmaster/ui/theme/Theme.kt
    app/src/main/java/com/nhimz/vocabmaster/ui/components/DuoComponents.kt
  </files>
  <read_first>app/src/main/java/com/nhimz/vocabmaster/ui/theme/Theme.kt</read_first>
  <acceptance_criteria>
    - Dark mode palette darkens surfaces but keeps brand colors identical to light mode.
    - Duo3DCard and DuoSnackbar are implemented from scratch using Material3.
  </acceptance_criteria>
  <action>
    Update Theme.kt to define the Dark Mode palette. Darken the surface colors but keep the brand colors (SuccessGreen, GradientGoldStart) identical to Light Mode per user decision D-03. Create DuoComponents.kt. Implement Duo3DCard (content card with subtle bottom shadow) and DuoSnackbar (styled for error/info) from scratch using Material3 primitives per user decision D-01. Do not reuse legacy implementations.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
    <human-check>Preview Duo3DCard and DuoSnackbar in Studio to verify styling matches Duolingo aesthetic.</human-check>
  </verify>
  <done>Theme.kt updated and Duo3DCard/DuoSnackbar available in DuoComponents.kt</done>
</task>

<task type="auto">
  <name>Task 2: Refactor HomeScreen to Container/Content</name>
  <files>app/src/main/java/com/nhimz/vocabmaster/ui/screens/HomeScreen.kt</files>
  <read_first>app/src/main/java/com/nhimz/vocabmaster/ui/screens/HomeScreen.kt</read_first>
  <acceptance_criteria>
    - HomeScreen is split into stateful Container and stateless Content.
    - Forced unwraps and unsafe casts are removed.
    - Errors are propagated to DuoSnackbar.
    - Empty state shows "Chưa có bài học nào".
  </acceptance_criteria>
  <action>
    Split HomeScreen into HomeScreen (stateful Container managing ViewModels/Events) and HomeScreenContent (stateless UI). Replace all forced unwraps and raw unsafe casts with safe casts (as?). If a cast fails, log the stack trace using LocalLogger and emit a UiState.Error with the message "Không thể tải dữ liệu. Vui lòng khởi động lại ứng dụng." per user decision D-04. Handle empty data states by showing the text "Chưa có bài học nào" and a CTA button (per UI-SPEC). Show a shimmer skeleton placeholder during loading states. Use DuoSnackbar for non-blocking errors like "Đã xảy ra lỗi. Vui lòng thử lại sau." instead of dialogs per user decision D-05.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create app/src/androidTest/java/com/nhimz/vocabmaster/ui/screens/HomeScreenTest.kt first</automated>
    <human-check>Open HomeScreen on device, verify loading shimmer, empty state, and absence of crashes.</human-check>
  </verify>
  <done>HomeScreen cleanly separated, safe casts used, and UI edge cases handled.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor SettingsScreen to Container/Content</name>
  <files>app/src/main/java/com/nhimz/vocabmaster/ui/screens/SettingsScreen.kt</files>
  <read_first>app/src/main/java/com/nhimz/vocabmaster/ui/screens/SettingsScreen.kt</read_first>
  <acceptance_criteria>
    - SettingsScreen is split into stateful Container and stateless Content.
    - Unsafe casts are removed.
  </acceptance_criteria>
  <action>
    Split SettingsScreen into SettingsScreen (Container) and SettingsScreenContent (Stateless UI). Eliminate all forced unwraps and unsafe casts, replacing them with safe alternatives. Apply the new Theme typography and spacing scale to the layout. Ensure all interactable elements have a minimum 48dp height for accessibility.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>SettingsScreen separated and modernized without unsafe casts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→UI | User input on UI components |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
| T-03-01 | Information Disclosure | UI Error States | low | mitigate | Generic error messages in UI, stack traces sent to LocalLogger only. |
</threat_model>

<verification>
Ensure HomeScreen and SettingsScreen compile successfully and render without crashes.
</verification>

<success_criteria>
The Container/Content pattern is established for Home and Settings, and the base Theme/DuoComponents are created.
</success_criteria>

<output>
Create .planning/phases/03-compose-ui-refactoring-polish/03-01-SUMMARY.md when done
</output>