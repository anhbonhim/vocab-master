---
phase: 05-backend-infrastructure-content-api
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - backend/app/services/llm_service.py
  - backend/app/services/content_gen.py
  - backend/tests/test_llm_service.py
autonomous: true
requirements:
  - CONT-02
must_haves:
  truths:
    - Backend trigger calls Opencode API using asynchronous httpx client.
    - "Do not use synchronous libraries like requests for the Opencode API call (use httpx.AsyncClient)."
    - "API timeout or failure when calling Opencode go api. Mechanism: httpx timeout triggers exception; script logs error and aborts exercise generation."
  artifacts:
    - backend/app/services/llm_service.py
    - backend/app/services/content_gen.py
  key_links:
    - llm_service uses validate_llm_output from schema.
---

<objective>
Tích hợp LLM để tự động sinh bài tập từ bộ từ vựng thông qua Opencode API và kịch bản script.

Purpose: Backend có khả năng tự động sinh content học tập.
Output: Service gọi LLM và script content generator.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-infrastructure-content-api/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>LLM Service Integration</name>
  <files>backend/app/services/llm_service.py, backend/tests/test_llm_service.py</files>
  <behavior>
    - async `generate_exercises` calls Opencode via `httpx.AsyncClient` with proper timeout.
    - httpx TimeoutException is caught and handled gracefully (aborts generation).
  </behavior>
  <action>
    Implement `generate_exercises_from_llm(vocabulary_list)` using `httpx.AsyncClient(timeout=60.0)`.
    Per D-02: Use Opencode go api directly.
    Do not use synchronous libraries like requests for the Opencode API call (prohibition).
    Handle httpx HTTPStatusError and TimeoutException.
  </action>
  <verify>
    <automated>pytest backend/tests/test_llm_service.py -v</automated>
  </verify>
  <read_first>backend/app/config.py</read_first>
  <acceptance_criteria>Async LLM call is made without blocking, and timeouts are caught.</acceptance_criteria>
  <done>Async LLM call is made without blocking, and timeouts are caught.</done>
</task>

<task type="auto">
  <name>Content Generation Script</name>
  <files>backend/app/services/content_gen.py</files>
  <action>
    Create a script `content_gen.py` that orchestrates:
    1. Fetching vocabulary list.
    2. Calling `llm_service.generate_exercises_from_llm`.
    3. Passing the result to `validate_llm_output`.
    4. Saving the valid JSON string into `Lesson.exercises_data` (per D-01).
  </action>
  <verify>
    <automated>python -m py_compile backend/app/services/content_gen.py</automated>
  </verify>
  <read_first>backend/app/services/llm_service.py</read_first>
  <acceptance_criteria>Pipeline correctly wires LLM fetch, validation, and DB save.</acceptance_criteria>
  <done>Pipeline correctly wires LLM fetch, validation, and DB save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network Client | Outbound network calls to Opencode API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | DoS | LLM Service | medium | mitigate | Enforce 60-second timeout on all httpx.AsyncClient calls. |
</threat_model>
