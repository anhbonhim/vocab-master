---
phase: 05-backend-infrastructure-content-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/tests/__init__.py
  - backend/app/models/curriculum.py
  - backend/app/models/report.py
  - backend/app/schemas/curriculum.py
  - backend/app/schemas/report.py
  - backend/app/routers/curriculum.py
  - backend/app/routers/report.py
  - backend/app/main.py
  - backend/tests/test_curriculum.py
autonomous: true
requirements:
  - CONT-01
must_haves:
  truths:
    - Client có thể gọi API GET /api/v1/curriculum/topics để lấy danh sách Topic thành công.
    - Exercises được lưu linh hoạt dưới dạng JSON, không yêu cầu migration schema khi thêm loại bài tập mới.
    - "Unresolved edge: SQLite exercises_data exceeds cell size limits for extremely large lessons. (Accepted for now without explicit cap)"
  artifacts:
    - backend/app/models/curriculum.py
    - backend/app/routers/curriculum.py
  key_links:
    - curriculum router được include vào main.py
---

<objective>
Khởi tạo Curriculum Data Models (Topic, Lesson) và User Report Model cùng các API endpoints.

Purpose: Backend cần cung cấp API cho app tải bài học và ghi nhận báo lỗi.
Output: Các model ORM và endpoint GET/POST tương ứng.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-infrastructure-content-api/05-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end Curriculum Topics API</name>
  <files>backend/requirements.txt, backend/tests/__init__.py, backend/app/models/curriculum.py, backend/app/schemas/curriculum.py, backend/app/routers/curriculum.py, backend/app/main.py, backend/tests/test_curriculum.py</files>
  <behavior>
    - GET /api/v1/curriculum/topics returns empty list if no topics.
  </behavior>
  <action>
    Add `pytest` and `pytest-asyncio` to `backend/requirements.txt` and create `backend/tests/__init__.py`.
    Create the SQLAlchemy `Topic` model and Pydantic `TopicResponse` schema.
    Create the `curriculum.py` router with a GET endpoint for topics.
    Register the router in `main.py`.
    (Per D-01: we establish the base pattern here)
  </action>
  <verify>
    <automated>pytest backend/tests/test_curriculum.py -v</automated>
  </verify>
  <read_first>backend/app/main.py</read_first>
  <acceptance_criteria>The single happy path for fetching topics works end-to-end from DB to HTTP response and is committed.</acceptance_criteria>
  <done>The single happy path for fetching topics works end-to-end from DB to HTTP response and is committed.</done>
</task>

<task type="auto">
  <name>Lesson and Exercise models and API</name>
  <files>backend/app/models/curriculum.py, backend/app/schemas/curriculum.py, backend/app/routers/curriculum.py</files>
  <action>
    Add `Lesson` model with `exercises_data` as SQLAlchemy JSON column (per D-01).
    Create GET endpoint to fetch lessons by topic_id.
    Do not create relational tables for specific exercise types (prohibition).
  </action>
  <verify>
    <automated>grep -v '^#' backend/app/models/curriculum.py | grep -c exercises_data</automated>
  </verify>
  <reversibility rating="costly">Lưu Exercises dạng JSON trong SQLite (D-01)</reversibility>
  <read_first>backend/app/models/curriculum.py</read_first>
  <acceptance_criteria>Lesson model includes JSON field for exercises and is exposed via API.</acceptance_criteria>
  <done>Lesson model includes JSON field for exercises and is exposed via API.</done>
</task>

<task type="auto">
  <name>User Report model and endpoint</name>
  <files>backend/app/models/report.py, backend/app/schemas/report.py, backend/app/routers/report.py</files>
  <action>
    Create `UserReport` ORM model and `ReportCreate` Pydantic schema (per D-03).
    Create POST /api/v1/reports endpoint to save reports to DB.
    Integrate Firebase auth middleware (e.g., `get_current_user_uid`) to secure the endpoint and associate the report with the authenticated user.
  </action>
  <verify>
    <automated>grep -v '^#' backend/app/models/report.py | grep -c UserReport</automated>
  </verify>
  <read_first>backend/app/models/curriculum.py</read_first>
  <acceptance_criteria>User report endpoint exists, requires authentication, and saves to DB.</acceptance_criteria>
  <done>User report endpoint exists, requires authentication, and saves to DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client API | API endpoints exposed to the frontend (Topics, Lessons, Reports). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | User Report Endpoint | medium | mitigate | Validate report input with Pydantic; ensure schema strictness. Require Firebase authentication. |
</threat_model>