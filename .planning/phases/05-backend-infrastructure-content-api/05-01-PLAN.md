---
phase: 05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/curriculum.py
  - backend/app/schemas/curriculum.py
  - backend/app/routers/curriculum.py
  - backend/app/models/report.py
  - backend/app/schemas/report.py
  - backend/app/routers/report.py
  - backend/app/main.py
  - backend/tests/test_curriculum.py
  - backend/tests/test_report.py
autonomous: true
requirements:
  - CONT-01
must_haves:
  truths:
    - Client có thể truy xuất danh sách Topic và Lesson qua HTTP GET.
    - Báo cáo lỗi (User Report) được lưu trữ thành công vào CSDL.
  artifacts:
    - backend/app/models/curriculum.py
    - backend/app/models/report.py
    - backend/app/routers/curriculum.py
    - backend/app/routers/report.py
  key_links:
    - main.py import curriculum và report models trước khi gọi metadata.create_all.
    - main.py mount routers của curriculum và report.
---

<objective>
Khởi tạo Curriculum Data Models (Topic, Lesson) và User Report Model cùng các API endpoints tương ứng để client truy xuất bài học và gửi báo cáo lỗi.

Purpose: Thiết lập nền tảng lưu trữ JSON linh hoạt cho bài tập (D-01) và bảng chứa User Report (D-03).
Output: Các model SQLAlchemy, Pydantic schemas, và FastAPI routers tương ứng.
</objective>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted input (User Report) crosses into backend. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | User Report Endpoint | medium | mitigate | Sử dụng `get_current_user_uid` từ Firebase auth để xác thực người gửi. |
</threat_model>

<tasks>

<task type="tracer">
  <name>Tracer: Curriculum API (Topics & Lessons)</name>
  <files>backend/app/models/curriculum.py, backend/app/schemas/curriculum.py, backend/app/routers/curriculum.py, backend/app/main.py, backend/tests/test_curriculum.py</files>
  <action>
    - Tạo SQLAlchemy models `Topic` (id, title, description) và `Lesson` (id, topic_id, title, exercises_data là JSON) trong `models/curriculum.py`.
    - Tạo Pydantic schemas `TopicResponse`, `LessonResponse` trong `schemas/curriculum.py`.
    - Tạo `routers/curriculum.py` với endpoint `GET /api/v1/curriculum/topics` và `GET /api/v1/curriculum/lessons/{topic_id}`.
    - Móc nối vào `main.py`: `app.include_router(curriculum.router)` và thêm `import app.models.curriculum` để `create_all` nhận diện bảng.
    - Viết test bằng `TestClient` trong `test_curriculum.py` (cấu hình dùng SQLite in-memory).
    - <reversibility rating="costly">Sử dụng JSON column cho exercises_data per D-01</reversibility>
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=. pytest tests/test_curriculum.py -v</automated>
  </verify>
  <done>
    - GET /api/v1/curriculum/topics trả về JSON array hợp lệ.
    - Models được khởi tạo thành công trong Database.
  </done>
</task>

<task type="auto">
  <name>Expansion: User Report API</name>
  <files>backend/app/models/report.py, backend/app/schemas/report.py, backend/app/routers/report.py, backend/app/main.py, backend/tests/test_report.py</files>
  <action>
    - Tạo model `UserReport` trong `models/report.py` (gồm user_uid, question_id, message, created_at) per D-03.
    - Tạo Pydantic schema `ReportCreate` trong `schemas/report.py`.
    - Xây dựng `POST /api/v1/reports` trong `routers/report.py`, dùng `Depends(get_current_user_uid)` để bảo mật endpoint.
    - Móc nối router và import model vào `main.py`.
    - Viết test trong `test_report.py` sử dụng override dependency để mock auth.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=. pytest tests/test_report.py -v</automated>
  </verify>
  <done>
    - Gửi POST request hợp lệ lưu được UserReport vào SQLite và trả về 201 Created.
  </done>
</task>

</tasks>

<verification>
- Cả hai test suite (curriculum và report) đều pass.
- Bảng topics, lessons, user_reports được sinh ra tự động khi khởi động app.
</verification>

<success_criteria>
- Backend có endpoints cấp phát dữ liệu Curriculum và tiếp nhận Feedback.
- API bảo mật đúng uỷ quyền Firebase.
</success_criteria>

<output>
Create .planning/phases/05-backend-infrastructure-content-api/05-01-SUMMARY.md when done
</output>