---
phase: 05
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/config.py
  - backend/app/schemas/llm.py
  - backend/tests/test_llm_schemas.py
autonomous: false
requirements:
  - CONT-03
must_haves:
  truths:
    - Package `httpx` được cài đặt sau khi người dùng verify an toàn.
    - Schema LLM loại bỏ được Markdown wrappers và parse thành công cấu trúc JSON.
  artifacts:
    - backend/app/schemas/llm.py
    - backend/tests/test_llm_schemas.py
  key_links:
    - Pydantic models trong llm.py chứa logic làm sạch json block trước khi validate.
---

<objective>
Kiểm định package cài đặt, cấu hình biến môi trường và xây dựng hệ thống Pydantic schemas cực kỳ nghiêm ngặt để chuẩn hóa dữ liệu sinh ra từ LLM.

Purpose: Đảm bảo backend không bao giờ lưu trữ dữ liệu rác (bọc trong markdown) hoặc sai cấu trúc từ LLM (CONT-03).
Output: Yêu cầu cài đặt đã được cấp phép, biến môi trường và Schema Validation vững chắc.
</objective>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internet -> Backend | Third party package (httpx) installed into environment. |
| LLM -> Backend | Output từ LLM không có cấu trúc cố định, dễ hỏng. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Cần blocking human checkpoint cho `httpx` trước khi install. |
| T-05-03 | Tampering | LLM Schema Validation | medium | mitigate | Sử dụng Pydantic `@model_validator` để ép kiểu và làm sạch JSON payload. |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Package Legitimacy Audit</name>
  <what-built>Kiểm định tính an toàn của thư viện httpx trước khi đưa vào requirements.txt</what-built>
  <how-to-verify>
    - Truy cập https://pypi.org/project/httpx/
    - Xác nhận đây là package chuẩn, phổ biến và an toàn để sử dụng cho async HTTP requests.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>LLM Schema Validation & Config Setup</name>
  <files>backend/requirements.txt, backend/app/config.py, backend/app/schemas/llm.py, backend/tests/test_llm_schemas.py</files>
  <behavior>
    - Test 1: Chuỗi JSON chuẩn chứa danh sách exercises parse thành công.
    - Test 2: Chuỗi JSON bị bọc bởi ```json \n ... \n ``` tự động được làm sạch và parse thành công.
    - Test 3: JSON thiếu các trường bắt buộc (ví dụ thiếu 'type') sẽ raise ValidationError.
  </behavior>
  <action>
    - Cập nhật `backend/requirements.txt`: thêm `httpx==0.27.0` (cùng với `pytest` nếu chưa có).
    - Mở rộng `Settings` trong `backend/app/config.py` để chứa `OPENCODE_API_KEY: str = ""`.
    - Tạo models `ExerciseItem` (type, question, correct_answer, options) và `LLMResponse` trong `schemas/llm.py`.
    - Thêm một `@model_validator(mode='before')` vào `LLMResponse` để tiền xử lý loại bỏ các ký tự Markdown block trước khi Pydantic parse JSON.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=. pytest tests/test_llm_schemas.py -v</automated>
  </verify>
  <done>
    - Pydantic models chặn đứng mọi input JSON hỏng và tự động làm sạch JSON hợp lệ bọc trong markdown.
  </done>
</task>

</tasks>

<verification>
- Test `test_llm_schemas.py` pass hoàn toàn, chứng minh schema hoạt động đúng như thiết kế TDD.
</verification>

<success_criteria>
- Backend sẵn sàng thư viện để gọi LLM và có hàng rào phòng thủ dữ liệu rác.
</success_criteria>

<output>
Create .planning/phases/05-backend-infrastructure-content-api/05-02-SUMMARY.md when done
</output>