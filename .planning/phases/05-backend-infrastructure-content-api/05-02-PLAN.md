---
phase: 05-backend-infrastructure-content-api
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - backend/requirements.txt
  - backend/app/config.py
  - backend/app/schemas/llm.py
  - backend/tests/test_schemas.py
autonomous: false
requirements:
  - CONT-03
must_haves:
  truths:
    - LLM JSON parsing is strictly validated using Pydantic V2 models.
    - "Do not save raw LLM responses directly to the database without Pydantic validation."
    - "Opencode API (LLM) returns empty, malformed JSON, or non-JSON text. Mechanism: Pydantic validation raises ValidationError; system retries or falls back safely."
  artifacts:
    - backend/app/schemas/llm.py
  key_links:
    - LLM output must pass through LLMResponse.model_validate_json
---

<objective>
Kiểm định package cài đặt, cấu hình biến môi trường và xây dựng hệ thống Pydantic schemas LLM.

Purpose: Đảm bảo dữ liệu sinh ra từ LLM hợp lệ trước khi lưu vào SQLite.
Output: Config env và schema validation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-infrastructure-content-api/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Verify httpx legitimacy</name>
  <what-built>Preparing to install httpx package which was marked [ASSUMED] in RESEARCH.md.</what-built>
  <how-to-verify>
    Check github.com/encode/httpx and PyPI for package legitimacy.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installation.</resume-signal>
  <read_first>backend/requirements.txt</read_first>
  <acceptance_criteria>httpx package is confirmed legitimate by human.</acceptance_criteria>
  <done>httpx package is confirmed legitimate by human.</done>
</task>

<task type="auto">
  <name>Install dependencies and setup config</name>
  <files>backend/requirements.txt, backend/app/config.py</files>
  <action>
    Add `httpx==0.27.0` to `requirements.txt`.
    Update `config.py` Settings class to include `OPENCODE_API_KEY` (with defaults or Env loading).
  </action>
  <verify>
    <automated>grep -v '^#' backend/requirements.txt | grep -c httpx && grep -v '^#' backend/app/config.py | grep -c OPENCODE_API_KEY</automated>
  </verify>
  <read_first>backend/app/config.py</read_first>
  <acceptance_criteria>httpx is in requirements and config parses the new LLM env vars.</acceptance_criteria>
  <done>httpx is in requirements and config parses the new LLM env vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Pydantic LLM Schemas for Exercise Validation</name>
  <files>backend/app/schemas/llm.py, backend/tests/test_schemas.py</files>
  <behavior>
    - Valid JSON payload parses into LLMResponse model containing a list of ExerciseItem.
    - Missing required fields raises ValidationError.
    - Malformed JSON string raises ValueError.
  </behavior>
  <action>
    Create Pydantic schemas (`ExerciseItem`, `LLMResponse`) to strongly type the expected JSON from the LLM.
    Implement a helper `validate_llm_output(json_str: str)` that manually strips markdown blocks from the string BEFORE passing it to `model_validate_json` to avoid Pydantic V2 validation errors.
    Do not save raw LLM responses directly to the database without Pydantic validation (prohibition).
  </action>
  <verify>
    <automated>pytest backend/tests/test_schemas.py -v</automated>
  </verify>
  <read_first>backend/app/schemas/curriculum.py</read_first>
  <acceptance_criteria>Validation strictly rejects malformed LLM responses and correctly parses valid ones after stripping markdown.</acceptance_criteria>
  <done>Validation strictly rejects malformed LLM responses and correctly parses valid ones after stripping markdown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External LLM | AI responses are untrusted input from an external system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
| T-05-02 | Tampering | LLM Schema | medium | mitigate | Pydantic strict validation of all AI outputs before DB insertion. |
</threat_model>