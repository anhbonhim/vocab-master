---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncResult.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/SettingsViewModel.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/screen/SettingsScreen.kt
  - app/src/test/java/com/nhimz/vocabmaster/ui/viewmodel/SettingsViewModelTest.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/local/dao/VocabDao.kt
  - data/src/test/java/com/nhimz/vocabmaster/data/sync/SyncManagerTest.kt
autonomous: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "Network failures during sync do not crash the app, but display an error state"
    - "Users can manually retry a failed sync from the Settings screen"
    - "Newer local card states are not overwritten by older server data during pull"
    - "Local review logs are preserved if pushing them to the server fails"
  artifacts:
    - "data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncResult.kt"
    - "data/src/test/java/com/nhimz/vocabmaster/data/sync/SyncManagerTest.kt"
  key_links:
    - "SettingsViewModel observes SyncManager's return type and emits state"
    - "VocabDao uses @Transaction for time-based merge"
---

<objective>
Implement network request resilience and data integrity for synchronization.
Purpose: Ensure sync failures are handled gracefully without crashing or losing data, and that bidirectional syncing respects FSRS state timestamps to prevent data downgrade.
Output: Hardened SyncManager, resilient UI feedback in Settings, and verified timestamp merge logic.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/data/com.termux/files/home/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "Manual Sync Retry" — one path only</name>
  <files>data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt, data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncResult.kt, app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/SettingsViewModel.kt, app/src/main/java/com/nhimz/vocabmaster/ui/screen/SettingsScreen.kt, app/src/test/java/com/nhimz/vocabmaster/ui/viewmodel/SettingsViewModelTest.kt</files>
  <action>Create `SyncResult` sealed class (Success, Error). Update `SyncManager.performSync()` to wrap Retrofit API calls in `runCatching`, catching `IOException`/`HttpException` and returning `SyncResult.Error` per D-01 and SYNC-01. Update `SettingsViewModel` to consume this result and expose a `SyncState.Failed` state to the UI. Update `SettingsScreen` to display a manual "Retry" button when in failed state (D-01, D-05). Ensure offline FSRS functionality remains perfectly intact (D-02).</action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*SettingsViewModelTest*"</automated>
  </verify>
  <done>Network errors propagate safely to the UI layer as an explicit state, showing a retry button without crashing the application.</done>
</task>

<task type="auto" tdd="true">
  <name>Implement Time-Based Merge and Safe Push (SYNC-02)</name>
  <files>data/src/main/java/com/nhimz/vocabmaster/data/local/dao/VocabDao.kt, data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt, data/src/test/java/com/nhimz/vocabmaster/data/sync/SyncManagerTest.kt</files>
  <behavior>
    - Test 1: SyncManager network failure returns Result.failure (SYNC-01)
    - Test 2: Local card with newer timestamp is not overwritten by older server payload (D-03)
    - Test 3: Review logs are not deleted locally if pushSync backend request fails (D-04)
  </behavior>
  <action>Implement time-based merging in `VocabDao` within a `@Transaction`: compare `updatedAt` (or `last_review`) between the incoming remote payload and the local card state; overwrite only if the remote payload is newer per D-03. In `SyncManager.pushSync`, execute the Retrofit API call first, and ONLY mark or delete local `ReviewLog` entries if the HTTP response is 2xx Success per D-04. Write tests in `SyncManagerTest` to verify these behaviors.</action>
  <verify>
    <automated>./gradlew :data:testDebugUnitTest --tests "*SyncManagerTest*"</automated>
  </verify>
  <done>Syncing uses timestamp-based conflict resolution and local review logs are never deleted on network push failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Sync payload from backend API crosses into local database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `SyncManager` | high | mitigate | Use safe type mapping (Gson/Moshi) and `runCatching` for all parsing/network errors. |
| T-04-02 | Denial of Service | `VocabDao` | high | mitigate | Wrap data merge and overwrite logic in Room `@Transaction` for atomic rollback. |
</threat_model>

<verification>
- SyncManager tests pass and prove resilient against HTTP and IO exceptions.
- Time-based conflict resolution favors the newest timestamp.
</verification>

<success_criteria>
- App gracefully degrades on network loss.
- FSRS card states and review logs are mathematically identical after a failed sync.
</success_criteria>

<output>
Create .planning/phases/04-sync-integration-verification/04-01-SUMMARY.md when done
</output>