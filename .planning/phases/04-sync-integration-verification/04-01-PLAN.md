---
phase: 04-sync-integration-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/SettingsViewModel.kt
  - data/src/test/java/com/nhimz/vocabmaster/data/sync/SyncManagerTest.kt
autonomous: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - SyncManager gracefully handles network failures from apiClient (SYNC-01)
    - Failed push syncs do not cause existing local review logs to be deleted (SYNC-02)
    - Pulled cards overwrite local cards only if they are newer (Time-Based Merging) (SYNC-02)
    - SettingsViewModel handles sync failure states and emits error SnackbarMessage
  artifacts:
    - data/src/test/java/com/nhimz/vocabmaster/data/sync/SyncManagerTest.kt
  key_links:
    - SyncManager -> apiClient error handling
    - SyncManager -> VocabDao timestamp comparison logic
---

<objective>
Implement network request resilience and time-based data integrity for synchronization in `SyncManager`.

Purpose: Secure the sync layer to handle transient failures gracefully without crashing, bubble up errors for user interaction (Explicit Retry UI), and prevent the FSRS database state from being inadvertently downgraded or corrupted by older server payloads.
Output: Hardened `SyncManager`, updated `SettingsViewModel` consuming it properly, and `SyncManagerTest`.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/data/com.termux/files/home/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sync-integration-verification/04-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "Sync Network Resilience" — one path only</name>
  <files>data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt, app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/SettingsViewModel.kt, data/src/test/java/com/nhimz/vocabmaster/data/sync/SyncManagerTest.kt</files>
  <behavior>
    - Test 1: sync() catches network exception (e.g. UnknownHostException) and returns false instead of crashing.
    - Test 2: failed sync() emits a SnackbarMessage in SettingsViewModel.
  </behavior>
  <read_first>
    - data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt
    - app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/SettingsViewModel.kt
  </read_first>
  <action>
    Modify `SyncManager.kt`:
    1. Wrap the entire contents of `sync()` in `return runCatching { ... }.getOrElse { Log.e(...); false }` (or keep the existing `try/catch(e: Exception)` but ensure it correctly intercepts Retrofit `IOException` / `HttpException` during `pushSync` and `pullSync`).
    2. Explicitly handle the `!pushResponse.isSuccessful` and `!pullResponse.isSuccessful` branches to return `false` cleanly without throwing unhandled exceptions to the ViewModel.
    
    Modify `SettingsViewModel.kt`:
    3. Ensure `triggerSync()` checks the boolean result of `syncManager.sync()`.
    4. On `false`, update `_uiState` to reflect `syncSuccess = false` and `syncError`, AND call `emitSnackbar(SnackbarMessage(text = ..., isError = true))` (per D-01/D-05).
    5. The existing codebase may already do this; if so, write tests to assert it.
    
    Create `data/src/test/java/com/nhimz/vocabmaster/data/sync/SyncManagerTest.kt`:
    6. Write a JUnit4 test `testSyncNetworkFailure()` that mocks `ApiClient` to throw an `IOException` or return a 500 Response for `pushSync`.
    7. Assert that `syncManager.sync()` returns `false` and does not throw.
  </action>
  <verify>
    <automated>./gradlew :data:testDebugUnitTest --tests "*SyncManagerTest.testSyncNetworkFailure*"</automated>
  </verify>
  <done>SyncManager intercepts API/network failures, returning false, and is tested via testSyncNetworkFailure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task: Implement Server-wins with Time-Based Merging and Log Preservation</name>
  <files>data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt, data/src/test/java/com/nhimz/vocabmaster/data/sync/SyncManagerTest.kt</files>
  <behavior>
    - Test 1: Pulling a card payload with an older `lastModified` (or `lastReview`) timestamp than the local FSRS card skips the update.
    - Test 2: Failing to push logs does NOT delete them from the local database.
  </behavior>
  <read_first>
    - data/src/main/java/com/nhimz/vocabmaster/data/remote/SyncPayload.kt
    - data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDao.kt
  </read_first>
  <action>
    Modify `SyncManager.kt`:
    1. In the `pullResponse.isSuccessful` block where cards are updated, find the timestamp comparison logic (D-03).
    2. Add logic: Compare the incoming card's `lastModified` timestamp with the local `existing` card's `lastReview` or assume a local timestamp mechanism.
    *Wait, `FsrsCardEntity` doesn't have `lastModified`!* The payload `VocabularyCardDto` has `lastModified: Long`. The `FsrsCardEntity` has `lastReview: Long?`.
    3. Rule (D-03): If `existing.lastReview != null` and incoming `c.lastModified < existing.lastReview`, **SKIP** the update to prevent downgrading FSRS state.
    4. Room transactions: Annotate `sync()` with `@Transaction`? No, `SyncManager` isn't a DAO. Ensure data operations are safe (Room's single DAOs handle simple queries).
    5. Review Logs (D-04): Ensure we only pull/insert review logs if they don't exist. Ensure local logs are *never* deleted on a failed `pushSync`. (The current code doesn't delete them, which is good, just write a test to prove it).
    
    Update `SyncManagerTest.kt`:
    6. Add `testTimeBasedMerging()`: Mock `vocabDao.getAllCards()` to return a card with `lastReview = 200`. Mock `pullSync()` to return a payload with `lastModified = 100`. Verify `vocabDao.updateFsrsCard` is NEVER called for that card.
    7. Add `testTimeBasedMerging_serverWins()`: Mock `pullSync` with `lastModified = 300`. Verify `updateFsrsCard` IS called.
  </action>
  <verify>
    <automated>./gradlew :data:testDebugUnitTest --tests "*SyncManagerTest*"</automated>
  </verify>
  <done>SyncManager uses timestamp checks before overwriting local FSRS state, verified by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network | Untrusted server payloads crossing into local Room database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `SyncManager` | high | mitigate | Implement Server-wins with Time-Based Merging to prevent stale payloads from downgrading local FSRS progress. |
| T-04-02 | Denial of Service | `SyncManager` | medium | mitigate | Wrap network calls in `runCatching` to prevent app crash on timeout/unknown host. |
</threat_model>

<verification>
1. All unit tests in `SyncManagerTest` pass.
2. `./gradlew testDebugUnitTest` runs green.
</verification>

<success_criteria>
1. `SyncManager` recovers from network errors and returns boolean result cleanly.
2. `SettingsViewModel` consumes the result and pushes Snackbar errors.
3. Time-based merging prevents stale payloads from overwriting local review state.
</success_criteria>

<output>
Create .planning/phases/04-sync-integration-verification/04-01-SUMMARY.md when done
</output>
