---
phase: 01-security-database-stabilization
plan: 06
type: execute
wave: 5
depends_on: ["01-05", "01-07"]
files_modified:
  - app/src/main/java/com/nhimz/vocabmaster/ui/screens/QuizScreen.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/screens/DebugPanelScreen.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/screens/statistics_components/OverviewTab.kt
  - domain/src/test/java/com/nhimz/vocabmaster/domain/usecase/UseCasesTest.kt
  - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/FSRS.kt
  - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/Models.kt
  - domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/FSRSTest.kt
  - domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/reference/ReferenceFSRS.kt
  - domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/reference/ReferenceModels.kt
  - data/src/test/java/com/nhimz/vocabmaster/data/database/VocabDaoTest.kt
autonomous: true
requirements:
  - PERS-02
  - PERS-03
must_haves:
  truths:
    - "The legacy FSRS implementation and its dedicated test harness are deleted — golden-vector + parity suites (Plans 01-02) are the sole FSRS test contracts (D-04 deletion gate: only after v6 passes all unit tests, which wave ordering guarantees)"
    - "Every remaining app/test file imports FSRS enums from domain.fsrs.v6 — the legacy domain.fsrs package is empty of production types"
    - "DAO behavior is proven by real-SQLite JVM tests: CRUD, Flow reads, review-log ordering, and recordReview atomicity (rollback on mid-transaction failure) (PERS-02/PERS-03, D-22)"
    - "The domain module contains zero string-format math — FSRS-03 is enforced repo-wide in domain, not just in v6"
  artifacts:
    - data/src/test/java/com/nhimz/vocabmaster/data/database/VocabDaoTest.kt
    - Deleted: FSRS.kt, Models.kt, FSRSTest.kt, reference/ReferenceFSRS.kt, reference/ReferenceModels.kt
  key_links:
    - "VocabDaoTest exercises VocabDatabase v8 entities + ReviewRepositoryImpl.recordReview rollback against in-memory Room (D-22 harness from Plan 04)"
    - "Enum migration (QuizScreen, DebugPanelScreen, OverviewTab, UseCasesTest) unblocks legacy Models.kt deletion"
---

<objective>
Final cleanup and proof: migrate the last legacy-enum consumers to v6, delete the legacy FSRS implementation and its superseded tests (D-04), and add the DAO/transaction test suite against the final v8 schema (PERS-02/03 closure).

Purpose: Sequenced last (wave 5) so the deletions happen only after the v6 suite is green in production use (Plan 05) and all file-level conflicts with Plan 07 are resolved.
Output: v6 as the single FSRS implementation; VocabDaoTest green; repo-wide domain string-format audit clean.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/data/com.termux/files/home/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-database-stabilization/01-CONTEXT.md
@.planning/phases/01-security-database-stabilization/01-05-PLAN.md
@app/src/main/java/com/nhimz/vocabmaster/ui/screens/QuizScreen.kt
@app/src/main/java/com/nhimz/vocabmaster/ui/screens/DebugPanelScreen.kt
@app/src/main/java/com/nhimz/vocabmaster/ui/screens/statistics_components/OverviewTab.kt
@domain/src/test/java/com/nhimz/vocabmaster/domain/usecase/UseCasesTest.kt
@data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDao.kt
@data/src/main/java/com/nhimz/vocabmaster/data/repository/ReviewRepositoryImpl.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate remaining enum consumers to v6 + delete legacy FSRS implementation (D-04)</name>
  <files>app/src/main/java/com/nhimz/vocabmaster/ui/screens/QuizScreen.kt, app/src/main/java/com/nhimz/vocabmaster/ui/screens/DebugPanelScreen.kt, app/src/main/java/com/nhimz/vocabmaster/ui/screens/statistics_components/OverviewTab.kt, domain/src/test/java/com/nhimz/vocabmaster/domain/usecase/UseCasesTest.kt, domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/FSRS.kt, domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/Models.kt, domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/FSRSTest.kt, domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/reference/ReferenceFSRS.kt, domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/reference/ReferenceModels.kt</files>
  <read_first>
    - app/src/main/java/com/nhimz/vocabmaster/ui/screens/QuizScreen.kt (legacy `domain.fsrs.Rating` import — used for the fsrsRating parameter)
    - app/src/main/java/com/nhimz/vocabmaster/ui/screens/DebugPanelScreen.kt (legacy `domain.fsrs.State` import — state-count display)
    - app/src/main/java/com/nhimz/vocabmaster/ui/screens/statistics_components/OverviewTab.kt (legacy `domain.fsrs.State` import)
    - domain/src/test/java/com/nhimz/vocabmaster/domain/usecase/UseCasesTest.kt (legacy `domain.fsrs.Rating` import; MapRatingUseCase already returns v6 Rating since Plan 05)
    - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/Models.kt (confirm nothing else still imports it before deletion)
  </read_first>
  <action>
    1. Swap the legacy enum imports to v6 in the four consumer files: `com.nhimz.vocabmaster.domain.fsrs.Rating` → `com.nhimz.vocabmaster.domain.fsrs.v6.Rating` (QuizScreen, UseCasesTest) and `com.nhimz.vocabmaster.domain.fsrs.State` → `com.nhimz.vocabmaster.domain.fsrs.v6.State` (DebugPanelScreen, OverviewTab). Names and int values are identical — these are pure import swaps; fix any fully-qualified references the same way.
    2. Repo-wide check: search all Kotlin sources for remaining references to the legacy package types (the legacy fully-qualified scheduler import and any `domain.fsrs.Card`/`ReviewLog`/`Rating`/`State` imports outside v6). Only the files scheduled for deletion may still contain them.
    3. Delete: `domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/FSRS.kt`, `domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/Models.kt`, `domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/FSRSTest.kt`, and the entire `domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/reference/` directory. Use `git rm` so deletions are staged.
    4. Run the domain string-format audit: the whole `domain/src/main` tree must contain no string-formatting calls in math code (FSRS-03 closure — legacy FSRS.kt was the last offender).
  </action>
  <verify>
    <automated>./gradlew :domain:compileKotlin :app:compileDebugKotlin :domain:test && test "$(grep -rn 'String\.format' domain/src/main/java --include='*.kt' | wc -l)" -eq 0 && test "$(grep -rln 'domain\.fsrs\.FSRS\|domain\.fsrs\.Models' app data domain --include='*.kt' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The five legacy files/directories no longer exist (git status shows deletions)
    - QuizScreen.kt and UseCasesTest.kt import `domain.fsrs.v6.Rating`; DebugPanelScreen.kt and OverviewTab.kt import `domain.fsrs.v6.State`
    - Filtered search for legacy FSRS/Models references across app/data/domain returns zero files
    - `grep -rn 'String\.format' domain/src/main/java --include='*.kt'` returns zero lines
    - `./gradlew :domain:compileKotlin :app:compileDebugKotlin :domain:test` exits 0 (GoldenVectorTest, PyFsrsParityTest, OptimizerTest, UseCasesTest all green without the legacy implementation)
  </acceptance_criteria>
  <done>D-04 complete: v6 is the only FSRS implementation in the repo; FSRS-03 enforced across domain.</done>
</task>

<task type="auto">
  <name>Task 2: VocabDaoTest — DAO CRUD, Flow reads, review-log ordering, recordReview atomicity (D-22)</name>
  <files>data/src/test/java/com/nhimz/vocabmaster/data/database/VocabDaoTest.kt</files>
  <read_first>
    - data/src/test/java/com/nhimz/vocabmaster/data/database/VocabDatabaseSmokeTest.kt (harness pattern)
    - data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDao.kt (final v8 surface)
    - data/src/main/java/com/nhimz/vocabmaster/data/database/entity/FsrsCardEntity.kt (v8 shape: nullable S/D, step, millis)
    - data/src/main/java/com/nhimz/vocabmaster/data/database/entity/ReviewLogEntity.kt (v8 shape: reviewDatetime, reviewDuration)
    - data/src/main/java/com/nhimz/vocabmaster/data/repository/ReviewRepositoryImpl.kt (recordReview implementation under test)
    - .planning/phases/01-security-database-stabilization/01-RESEARCH.md (Pitfall 1: no dispatcher switches inside withTransaction)
  </read_first>
  <action>
    Create `data/src/test/java/com/nhimz/vocabmaster/data/database/VocabDaoTest.kt` (RobolectricTestRunner + inMemoryDatabaseBuilder with the test-only main-thread allowance + `runTest`, per D-22). Fresh DB per test via @Before/@After. Cover:
    - `insertAndReadCard_v8Shape`: insert an FsrsCardEntity (questionId "q1", state = State.New.value, step = 0, stability = null, difficulty = null, due = some millis, lastReview = null) → `getCardByQuestionId("q1")` returns it with all fields intact (nulls preserved).
    - `updateCardPersistsV6Fields`: update the card to state = Review.value, step = null, stability = 12.5, difficulty = 5.6, lastReview = millis → re-read and assert.
    - `dueAndNewCardsFlow_emitsReactively`: collect `getDueAndNewCards(State.New.value, now, 10)` with `.first()` after inserting one due and one future card joined to their QuestionEntity rows (insert QuestionEntity fixtures too — the query JOINs questions) → only the due/new one returned. Also assert the function type compiles as a Flow read (PERS-02 shape) — calling `.first()` inside runTest is the proof.
    - `reviewLogOrderingAndDuration`: insert 3 ReviewLogEntity rows (different reviewDatetime, one with reviewDuration = null, others with values) → `getReviewLogs("q1")` returns them ordered by reviewDatetime DESC with durations intact.
    - `recordReview_isAtomic`: build `ReviewRepositoryImpl(database, vocabDao)` (constructor order per the impl); call recordReview(card, log) with a valid v6 Card + ReviewLog → both the card update and the log row are present. Then sabotage: pre-insert a ReviewLogEntity, then invoke recordReview with a log whose entity insert will fail (e.g., cardId violating the FK constraint to a non-existent card with foreign keys enforced — if FK enforcement is off by default in the in-memory builder, instead throw inside the transaction by using a dao spy or by attempting a second recordReview variant whose card update targets a row deleted mid-test; the simplest deterministic approach: call `database.withTransaction { vocabDao.updateFsrsCard(...); throw RuntimeException("boom") }` directly replicating recordReview's body and assert the card row is unchanged) → assert rollback: card retains its pre-transaction field values and no new log row exists. This proves PERS-03's atomicity claim.
    - `stateCountsAndLearnedCount`: insert cards across New/Learning/Review → `getStateCounts().first()` groups correctly and `getLearnedCount().first()` excludes New (state 0) — locks the v6 enum int mapping (New=0) against the DB.
  </action>
  <verify>
    <automated>./gradlew :data:testDebugUnitTest --tests "com.nhimz.vocabmaster.data.database.VocabDaoTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :data:testDebugUnitTest --tests "com.nhimz.vocabmaster.data.database.VocabDaoTest"` exits 0
    - Test file contains an atomicity test asserting the card row is unchanged after a mid-transaction exception AND that no orphan log row exists
    - Test file contains a Flow-read test using `.first()` inside `runTest` (proves the PERS-02 reactive read shape)
    - All tests use the v8 entity shapes (nullable stability/difficulty, step, reviewDuration, millis)
  </acceptance_criteria>
  <done>PERS-02/03 closure: DAO surface and the atomic card+log write proven against real SQLite semantics.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
(Plan 06 artifacts)
- `VocabDaoTest` (Robolectric DAO suite in com.nhimz.vocabmaster.data.database)
- v6 enum adoption in: QuizScreen, DebugPanelScreen, OverviewTab, UseCasesTest
- Deletions (negative artifacts): legacy `FSRS` class, legacy `Models.kt` (Card/ReviewLog/Rating/State), `FSRSTest`, `reference/ReferenceFSRS`, `reference/ReferenceModels`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → production DAO | In-memory Room must faithfully represent production SQLite semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | Tampering | Legacy buggy scheduler survives as a shadow implementation someone could still call | medium | mitigate | Task 1 deletes FSRS.kt/Models.kt + dedicated tests; acceptance grep proves zero residual references; v6 is the only importable scheduler |
| T-01-19 | Tampering | recordReview partially commits under failure (test blind spot) | high | mitigate | Task 2 atomicity test forces a mid-transaction exception and asserts full rollback of the card row + absence of the log row |
| T-01-20 | Denial of Service | Test-harness main-thread allowance masks a real threading violation | low | accept | D-22/D-23 explicitly allow the allowance ONLY in in-memory test builders; production guard verified in Plan 04 Task 2 |
</threat_model>

<verification>
- `./gradlew :app:compileDebugKotlin :domain:compileKotlin :domain:test :data:testDebugUnitTest` — everything green
- `./gradlew detekt` (all modules) — green
- Legacy-reference greps — zero hits
</verification>

<success_criteria>
- v6 is the sole FSRS implementation; golden + parity + optimizer + DAO suites all green
- Phase 1 success criteria 2 and 4 (FSRS tests green; coroutine/Flow DB layer) fully proven
</success_criteria>

<output>
Create `.planning/phases/01-security-database-stabilization/01-06-SUMMARY.md` when done. Include: deleted-file list, enum migration file list, and the VocabDaoTest case inventory.
</output>
