---
phase: 01-security-database-stabilization
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/v6/PyFsrsParityTest.kt
autonomous: true
requirements:
  - FSRS-01
  - FSRS-04
must_haves:
  truths:
    - "py-fsrs's own pytest behavioral cases are ported to Kotlin JUnit and pass against the v6 Scheduler (D-18)"
    - "Parity coverage includes exact golden values from py-fsrs tests: interval history [0,2,11,46,163,498,0,0,2,4,7,12,21] and memo-state stability 53.62691 / difficulty 6.3574867 (1e-4)"
    - "Parameter validation, reschedule_card, serde round-trips, stability lower bound, and maximum-interval behavior match py-fsrs semantics"
  artifacts:
    - domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/v6/PyFsrsParityTest.kt
  key_links:
    - "PyFsrsParityTest asserts against v6.Scheduler created in Plan 01"
    - "Test cases are ported from py-fsrs tests/test_basic.py (fetched at execution time)"
---

<objective>
Port py-fsrs's own pytest suite (`tests/test_basic.py`) to Kotlin JUnit as `PyFsrsParityTest.kt` (D-18). Golden vectors (Plan 01) cover the broad matrix; this suite covers py-fsrs maintainers' chosen edge cases — double coverage that catches drift if the port later diverges from py-fsrs.

Purpose: FSRS-01/FSRS-04 — prove the Kotlin port behaves identically to the reference on the reference's own regression tests.
Output: One green parity test class.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/data/com.termux/files/home/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-database-stabilization/01-CONTEXT.md
@.planning/phases/01-security-database-stabilization/01-01-PLAN.md
@domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/Scheduler.kt
@domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/Card.kt
@domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/ReviewLog.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port core behavioral parity cases (scheduling, learning steps, retrievability, bounds)</name>
  <files>domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/v6/PyFsrsParityTest.kt</files>
  <read_first>
    - py-fsrs test source — fetch at execution time (AGENTS.md web rules: curl with a browser User-Agent): `curl -s -A "Mozilla/5.0" "https://raw.githubusercontent.com/open-spaced-repetition/py-fsrs/main/tests/test_basic.py"` (a copy from planning may still exist at /data/data/com.termux/files/usr/tmp/opencode/pyfsrs_test_basic.py — prefer re-fetching)
    - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/Scheduler.kt
    - The ground_truth_decisions section of 01-01-PLAN.md (State.New alias: py `State.Learning`+step 0+null S/D ↔ v6 `State.New`; after first review the v6 card is Learning/Review/Relearning, never New)
  </read_first>
  <behavior>
    - ratingSeq Good×6,Again×2,Good×5 from pristine (fuzzing off) → interval history exactly [0,2,11,46,163,498,0,0,2,4,7,12,21]
    - Again,Good×5 with per-rating elapsed days [0,0,1,3,8,21] from pristine (fuzzing off) → stability ≈ 53.62691 (1e-4), difficulty ≈ 6.3574867 (1e-4)
    - 10× Easy same-second → difficulty == 1.0
    - 1000× Again one day apart → stability always >= 0.001
  </behavior>
  <action>
    Create `domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/v6/PyFsrsParityTest.kt` (JUnit 4, AAA, package `com.nhimz.vocabmaster.domain.fsrs.v6`). Port these cases from py-fsrs `tests/test_basic.py`, translating datetimes to epoch millis and remembering the State.New alias (assertions on a pristine v6 card check `State.New` and `step == 0`; after the first review assert the py-expected state by name):
    - `test_review_card`: Scheduler(enableFuzzing=false); rating sequence Good×6, Again×2, Good×5 starting 2022-11-29T12:30Z, each next review at card.due; assert interval-days history == [0,2,11,46,163,498,0,0,2,4,7,12,21] (interval = (due - lastReview) floor-div 86_400_000).
    - `test_memo_state`: ratings Again,Good,Good,Good,Good,Good with elapsed-day offsets [0,0,1,3,8,21] (review_datetime advances by that many days each step, fuzzing OFF for determinism — note py runs this with default fuzzing but asserts S/D only); assert stability ≈ 53.62691 (delta 1e-4) and difficulty ≈ 6.3574867 (delta 1e-4).
    - `test_repeated_correct_reviews`: 10 Easy reviews at the same second → difficulty == 1.0 exactly.
    - `test_retrievability`: pristine → retrievability == 0.0; after Good → Learning, R in [0,1]; after second Good at due → Review, R in [0,1]; after Again at due → Relearning, R in [0,1].
    - `test_good_learning_steps`: pristine + Good at due → Learning, step 1, due ≈ +10 min (assert delta within [9.5, 10.5] minutes); + Good at due → Review, step null, due >= +1 day.
    - `test_again_learning_steps`: pristine + Again → Learning, step 0, due ≈ +1 min ([55, 65] seconds).
    - `test_hard_learning_steps`: pristine + Hard → Learning, step 0, due ≈ +5.5 min ([5.0, 6.0] minutes) — the (steps[0]+steps[1])/2 branch.
    - `test_easy_learning_steps`: pristine + Easy → Review immediately, step null, due >= +1 day.
    - `test_review_state` and `test_relearning`: port the state-transition and interval assertions (Review→Again→Relearning step 0, relearning Good at final step → Review).
    - `test_no_learning_steps` / `test_no_relearning_steps`: Scheduler with empty step arrays → first Again goes straight to Review with interval >= 1 day; Review-state Again with no relearning steps stays Review with interval >= 1.
    - `test_maximum_interval`: Scheduler(maximumInterval = 100); grow a card's stability via repeated Good reviews spaced at due; assert (due - lastReview) <= 100 days always.
    - `test_stability_lower_bound`: 1000 Again reviews one day apart → stability >= 0.001 after every review.
    - `test_long_term_stability_learning_state`: port the long-term stability assertions for a Learning-state card reviewed after > 1 day.
    Skip with an explicit KDoc note listing them: `test_datetime` (UTC-awareness ValueError is unreachable — epoch-millis model), `test_unique_card_ids` (ids are caller-supplied Strings), `test_class_repr` (Python repr format), `test_fuzz` exact-seed values (cross-language PRNG parity impossible — see Task 2 property test instead), `test_repeat_default_arg` (wall-clock now — non-deterministic), optimizer lazy-loading tests (covered by Plan 03 tests).
  </action>
  <verify>
    <automated>./gradlew :domain:test --tests "com.nhimz.vocabmaster.domain.fsrs.v6.PyFsrsParityTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :domain:test --tests "com.nhimz.vocabmaster.domain.fsrs.v6.PyFsrsParityTest"` exits 0
    - Test class contains the exact literal interval list 0, 2, 11, 46, 163, 498, 0, 0, 2, 4, 7, 12, 21 and the approximate constants 53.62691 and 6.3574867
    - KDoc on the class lists the intentionally-skipped py-fsrs cases with reasons
    - Every ported py test name maps to a same-named @Test method (prefixed `test_`)
  </acceptance_criteria>
  <done>Core py-fsrs behavioral suite green against the Kotlin port.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port validation, serde, reschedule, and fuzz-property parity cases</name>
  <files>domain/src/test/java/com/nhimz/vocabmaster/domain/fsrs/v6/PyFsrsParityTest.kt</files>
  <read_first>
    - py-fsrs `tests/test_basic.py` sections: test_scheduler_parameter_validation, test_Card_dict_serialize, test_Card_json_serialize, test_ReviewLog_dict_serialize, test_ReviewLog_json_serialize, test_Scheduler_dict_serialize, test_Scheduler_json_serialize, test_custom_scheduler_args, test_one_card_multiple_schedulers, test_reschedule_card_* (5 cases), test_class___eq___methods, test_fuzz (same fetch command as Task 1)
    - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/Scheduler.kt
    - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/Card.kt
    - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/ReviewLog.kt
  </read_first>
  <behavior>
    - Scheduler(parameters with any out-of-bounds or wrong-length array) throws IllegalArgumentException
    - Card/ReviewLog/Scheduler survive dict and JSON round-trips with all fields intact
    - rescheduleCard with logs from another cardId throws IllegalArgumentException
    - With fuzzing enabled and seeded Random, fuzzed intervals always land inside the computed [minIvl, maxIvl] band
  </behavior>
  <action>
    Append to `PyFsrsParityTest.kt`:
    - `test_scheduler_parameter_validation`: valid defaults construct OK; parameters[6]=100 → IllegalArgumentException; parameters[10]=-42 → IAE; parameters[0]=0 and [3]=101 → IAE; empty array → IAE; 20 params → IAE; 24 params → IAE.
    - Serde round-trips (`test_card_dict_serialize`, `test_card_json_serialize`, `test_review_log_dict_serialize`, `test_review_log_json_serialize`, `test_scheduler_dict_serialize`, `test_scheduler_json_serialize`): build objects with non-default field values, round-trip through toDict/fromDict and toJson/fromJson, assert equality of every field. Assert JSON key names match py (`card_id`, `state`, `step`, `stability`, `difficulty`, `due`, `last_review`; `review_datetime`, `review_duration`; `parameters`, `desired_retention`, `learning_steps`, `relearning_steps`, `maximum_interval`, `enable_fuzzing`) and that learning/relearning steps serialize as SECONDS (60, 600 / 600). Document in KDoc that due/last_review/review_datetime serialize as epoch-millis numbers (deviation from py ISO strings, intentional).
    - `test_custom_scheduler_args` and `test_one_card_multiple_schedulers`: port constructor-argument handling and the two-schedulers-one-card behavior assertions.
    - `test_reschedule_card_same_scheduler`, `test_reschedule_card_different_parameters`, `test_reschedule_card_different_desired_retention`, `test_reschedule_card_different_learning_steps`: port the reschedule semantics (replay sorted logs from pristine; rescheduled card equals the naturally-scheduled card under the same scheduler; differs appropriately otherwise). `test_reschedule_card_wrong_review_logs`: logs whose cardId mismatches → IllegalArgumentException.
    - `test_class_eq_methods`: two Schedulers with different desiredRetention are not equal; identical configs are equal (compare via toDict() maps if Scheduler lacks equals — data-class equality applies to Card/ReviewLog; for Scheduler compare toDict()).
    - `test_fuzz_property`: Scheduler(enableFuzzing = true, random = kotlin.random.Random(seed)) for seeds 42 and 12345; drive a card to Review and schedule repeatedly; for each fuzzed interval assert (a) interval >= 2, (b) interval within the [minIvl, maxIvl] band recomputed from FUZZ_RANGES for the unfuzzed value, (c) interval <= maximumInterval, (d) same seed → same sequence (determinism). KDoc: exact-value fuzz parity with CPython's `random` is not portable; property parity is the contract (aligns with D-07 — fuzz is off by default in production).
  </action>
  <verify>
    <automated>./gradlew :domain:test --tests "com.nhimz.vocabmaster.domain.fsrs.v6.PyFsrsParityTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :domain:test --tests "com.nhimz.vocabmaster.domain.fsrs.v6.PyFsrsParityTest"` exits 0 with all Task 1 + Task 2 cases
    - Test file contains 6 parameter-validation assertions throwing IllegalArgumentException
    - Test file contains reschedule tests including the mismatched-cardId exception case
    - Serde tests assert py JSON key names including `card_id`, `review_datetime`, `desired_retention`, `enable_fuzzing`
  </acceptance_criteria>
  <done>Full parity suite green: validation, serde, reschedule, fuzz properties (D-18 complete).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
(Plan 02 artifacts)
- `com.nhimz.vocabmaster.domain.fsrs.v6.PyFsrsParityTest` (test class; ~25 @Test methods ported from py-fsrs tests/test_basic.py)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub raw → dev machine | test_basic.py fetched at execution time as the parity spec |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | Parity spec fetch (test_basic.py) could be stale/MITM-modified | low | mitigate | Fetch over HTTPS from the official open-spaced-repetition/py-fsrs repo only; golden values asserted in tests ([0,2,11,...], 53.62691) are independently anchored in this plan text, so a tampered spec fails loudly |
| T-01-04 | Denial of Service | 1000-iteration bounds test slows suite | low | accept | JVM executes 1000 scheduler steps in < 1s; acceptable cost for a hard floor guarantee |
</threat_model>

<verification>
- `./gradlew :domain:test --tests "com.nhimz.vocabmaster.domain.fsrs.v6.PyFsrsParityTest"` — green
- `./gradlew :domain:test` — whole domain suite green (GoldenVectorTest + PyFsrsParityTest + existing UseCasesTest + legacy FSRSTest)
</verification>

<success_criteria>
- Every portable case from py-fsrs tests/test_basic.py exists as a Kotlin @Test and passes
- Skipped cases are enumerated in KDoc with reasons (no silent omissions)
</success_criteria>

<output>
Create `.planning/phases/01-security-database-stabilization/01-02-SUMMARY.md` when done. Include the list of ported cases, the skipped cases + reasons, and any formula-level discrepancies discovered while porting the tests.
</output>
