---
phase: 01-security-database-stabilization
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - data/src/main/java/com/nhimz/vocabmaster/data/database/entity/FsrsCardEntity.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/database/entity/ReviewLogEntity.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/database/Converters.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDatabase.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDao.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/model/BackupModels.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/repository/VocabularyRepositoryImpl.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/repository/ReviewRepositoryImpl.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/repository/BackupRepositoryImpl.kt
  - data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt
  - domain/src/main/java/com/nhimz/vocabmaster/domain/model/QuestionWithCard.kt
  - domain/src/main/java/com/nhimz/vocabmaster/domain/model/VocabularyRepository.kt
  - domain/src/main/java/com/nhimz/vocabmaster/domain/model/ReviewRepository.kt
  - domain/src/main/java/com/nhimz/vocabmaster/domain/usecase/MapRatingUseCase.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/QuizViewModel.kt
  - app/src/main/java/com/nhimz/vocabmaster/notification/NotificationReceiver.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/MainViewModel.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/screens/debug_components/DataIntegrityTests.kt
  - app/src/main/java/com/nhimz/vocabmaster/ui/screens/statistics_components/MistakeBankTab.kt
autonomous: true
requirements:
  - FSRS-01
  - FSRS-02
  - FSRS-03
  - PERS-03
must_haves:
  truths:
    - "Runtime scheduling in the app goes through v6.Scheduler.reviewCard — the buggy legacy FSRS class has zero production call sites after this plan (FSRS-01/02/03 become runtime-true, not just test-true)"
    - "The Room schema (v8) stores all py-fsrs Card fields (state, step, stability?, difficulty?, due millis, last_review? millis) and the ReviewLog entity matches py-fsrs (rating, review_datetime millis, review_duration?, card_id FK + index) (D-09)"
    - "Card update + review-log insert commit atomically via db.withTransaction in ReviewRepositoryImpl.recordReview — a crash between the two writes can no longer desynchronize card state from its log (PERS-03, D-21)"
    - "All due-date comparisons use epoch millis consistently end-to-end (fixes the seconds/millis mix: MainViewModel.getDueCount previously passed millis against second-based rows, QuizViewModel/NotificationReceiver passed seconds)"
    - "Existing user scheduling data resets to New state via destructive migration on the v7→v8 bump (D-02); curriculum re-seeds automatically from assets on next access"
  artifacts:
    - FsrsCardEntity v8 (step: Int?, stability/difficulty: Double?, due/lastReview millis, keeps reps/lapses metadata)
    - ReviewLogEntity v8 (rating: Int, reviewDatetime: Long, reviewDuration: Long?, FK + index kept)
    - ReviewRepository.recordReview(card, log) + ReviewRepositoryImpl with VocabDatabase injection
    - BackupModels schema v3 (AppBackup.version = 3; import rejects < 3)
    - QuizViewModel scheduling block on v6.Scheduler with reviewDuration = responseTimeMs
  key_links:
    - "QuizViewModel → v6.Scheduler.reviewCard → ReviewRepository.recordReview → db.withTransaction { VocabDao.updateFsrsCard + VocabDao.insertReviewLog }"
    - "VocabDatabase version 8 + fallbackToDestructiveMigration (already configured in DataModule) → automatic reset to New on upgrade"
    - "FsrsCardEntity.toDomain/fromDomain ↔ v6.Card (int state values identical: New=0, Learning=1, Review=2, Relearning=3)"
---

<objective>
Atomic cutover: migrate the Room schema to the full py-fsrs shape (D-09), point all domain/data/app consumers at the v6 models created in Plan 01, and swap the app's only scheduler call site (QuizViewModel) to `v6.Scheduler.reviewCard` with an atomic card+log write (PERS-03). Legacy `FSRS.kt`/`Models.kt` are NOT deleted yet (Plan 06, after remaining enum consumers migrate) — but after this plan the legacy scheduler has zero production call sites.

Purpose: This is where FSRS-01/02/03 stop being test-only and become the app's actual behavior. Sequenced after Plan 02 per D-04 (production swap only after golden + parity suites pass).
Output: Build-green cutover across domain, data, and app; DB v8; atomic review persistence.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/data/com.termux/files/home/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-database-stabilization/01-CONTEXT.md
@.planning/phases/01-security-database-stabilization/01-01-PLAN.md
@data/src/main/java/com/nhimz/vocabmaster/data/database/entity/FsrsCardEntity.kt
@data/src/main/java/com/nhimz/vocabmaster/data/database/entity/ReviewLogEntity.kt
@data/src/main/java/com/nhimz/vocabmaster/data/database/Converters.kt
@data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDatabase.kt
@data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDao.kt
@data/src/main/java/com/nhimz/vocabmaster/data/model/BackupModels.kt
@data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt
@app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/QuizViewModel.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema v8 + domain/data cutover to v6 models (entities, DAO, repositories, backup v3, sync compile-fix)</name>
  <files>data/src/main/java/com/nhimz/vocabmaster/data/database/entity/FsrsCardEntity.kt, data/src/main/java/com/nhimz/vocabmaster/data/database/entity/ReviewLogEntity.kt, data/src/main/java/com/nhimz/vocabmaster/data/database/Converters.kt, data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDatabase.kt, data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDao.kt, data/src/main/java/com/nhimz/vocabmaster/data/model/BackupModels.kt, data/src/main/java/com/nhimz/vocabmaster/data/repository/VocabularyRepositoryImpl.kt, data/src/main/java/com/nhimz/vocabmaster/data/repository/ReviewRepositoryImpl.kt, data/src/main/java/com/nhimz/vocabmaster/data/repository/BackupRepositoryImpl.kt, data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt, domain/src/main/java/com/nhimz/vocabmaster/domain/model/QuestionWithCard.kt, domain/src/main/java/com/nhimz/vocabmaster/domain/model/VocabularyRepository.kt, domain/src/main/java/com/nhimz/vocabmaster/domain/model/ReviewRepository.kt, domain/src/main/java/com/nhimz/vocabmaster/domain/usecase/MapRatingUseCase.kt</files>
  <read_first>
    - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/Card.kt (target model — note reps/lapses metadata fields)
    - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/ReviewLog.kt
    - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/State.kt
    - data/src/main/java/com/nhimz/vocabmaster/data/database/entity/FsrsCardEntity.kt (current shape + mappers)
    - data/src/main/java/com/nhimz/vocabmaster/data/database/entity/ReviewLogEntity.kt
    - data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDao.kt (review_logs ORDER BY timestamp references must follow the column rename)
    - data/src/main/java/com/nhimz/vocabmaster/data/sync/SyncManager.kt (constructs both entities; builds ReviewLogDto from entity fields — see note below)
    - .planning/phases/01-security-database-stabilization/01-CONTEXT.md (D-02, D-09, D-21)
  </read_first>
  <action>
    1. `FsrsCardEntity` (tableName stays "fsrs_cards", PK stays `questionId: String`): fields become `due: Long` (epoch MILLIS), `stability: Double?`, `difficulty: Double?`, `step: Int?`, `state: Int`, `lastReview: Long?` (epoch millis, null when never reviewed), KEEP `reps: Int` and `lapses: Int` (app metadata consumed by mistake-bank queries; v6 Card carries them too), DROP `interval` (py-fsrs derives intervals from due − last_review; nothing may read this column after the cutover). Rewrite `toDomain()` → v6 `Card(cardId = questionId, state = State.entries.firstOrNull { it.value == state } ?: State.New, step = step, stability = stability, difficulty = difficulty, due = due, lastReview = lastReview, reps = reps, lapses = lapses)` and `fromDomain(card)` inverse (imports switch from `domain.fsrs.*` to `domain.fsrs.v6.*`).
    2. `ReviewLogEntity` (tableName "review_logs", keep FK to FsrsCardEntity.questionId with CASCADE and the cardId index): fields become `id: Long = 0` (auto), `cardId: String`, `rating: Int` (store Rating.value directly), `reviewDatetime: Long` (epoch millis), `reviewDuration: Long?`. Mappers to/from v6 `ReviewLog`. Update `VocabDao` review_logs queries: `ORDER BY timestamp` → `ORDER BY reviewDatetime` (3 occurrences: getReviewLogs, getReviewLogsFlow, getAllReviewLogsFlow/getAllReviewLogsList).
    3. `Converters.kt`: delete the LocalDateTime, Rating, and State converters (entities now store plain Long/Int); KEEP NodeType/QuestionType converters. `VocabDatabase.kt`: bump `version = 7` → `version = 8`. No Migration object — `fallbackToDestructiveMigration(dropAllTables = true)` in DataModule already handles the bump by wiping all tables (D-02 reset; curriculum + fresh New cards re-seed via ensureCurriculumAndFsrsSeeded on next access). Add a KDoc on VocabDatabase stating the v7→v8 destructive reset is intentional per D-02 (pre-launch).
    4. Domain cutover (import swaps `domain.fsrs.*` → `domain.fsrs.v6.*`, no logic changes): `QuestionWithCard.kt` (v6 Card), `VocabularyRepository.kt` (v6 Card), `ReviewRepository.kt` (v6 ReviewLog/State), `MapRatingUseCase.kt` (v6 Rating — same enum values, pure import swap). Legacy `domain/fsrs/Models.kt` stays for now (QuizScreen/DebugPanelScreen/OverviewTab still import its enums; Plan 06 removes it).
    5. `ReviewRepository`: add `suspend fun recordReview(card: Card, log: ReviewLog)`. `ReviewRepositoryImpl`: inject `VocabDatabase` alongside `VocabDao` (Hilt already provides the DB singleton via DataModule); implement recordReview as `database.withTransaction { vocabDao.updateFsrsCard(FsrsCardEntity.fromDomain(card)); vocabDao.insertReviewLog(ReviewLogEntity.fromDomain(log)) }` — no dispatcher switches inside the block (thread-confinement pitfall from RESEARCH); the entity carries the already-incremented reps/lapses from v6.reviewCard. Keep `insertReviewLog(cardId, log)` for SyncManager/other callers; v6 ReviewLog contains its own cardId — update the existing method's mapping accordingly.
    6. `VocabularyRepositoryImpl`: update all FsrsCardEntity constructions/mappings to the new shape. Seed path (`ensureCurriculumAndFsrsSeeded`): new cards get `due = System.currentTimeMillis()`, `stability = null`, `difficulty = null`, `step = 0`, `state = State.New.value`, `lastReview = null`, `reps = 0`, `lapses = 0` (v6 imports). `updateCard(card)` maps v6 fields (reps/lapses carried on the domain card). All `currentTimestamp` parameters are now epoch MILLIS — update KDoc on the interface methods (`getDueCards`, `getDueCardsScoped`, `getDueCount`, `getDueCardCountByUnit`) to say millis.
    7. `BackupModels.kt` → schema v3: `FsrsCardBackup(questionId, state: Int, step: Int?, stability: Double?, difficulty: Double?, due: Long /*millis*/, lastReview: Long? /*millis*/, reps: Int, lapses: Int)` (drop interval); `ReviewLogBackup(cardId, rating: String /*enum name*/, reviewDatetime: String /*ISO-8601 UTC*/, reviewDuration: Long?)` (drop elapsed_days/scheduled_days/stability/difficulty/state/id — no longer stored); `AppBackup.version = 3`. `BackupRepositoryImpl`: export maps new entity shapes (reviewDatetime millis → ISO via `java.time.Instant.ofEpochMilli(...).toString()`); import validates `backup.version < 3` → return false (old v2 files carry pre-port scheduling data — rejected, consistent with D-02 reset); import maps v3 → new entities inside the existing `database.withTransaction` (keep it — D-21).
    8. `SyncManager.kt` (compile-level adaptation ONLY — full sync contract verification is Phase 4 / SYNC-02): entity constructions use the new shapes (`FsrsCardEntity(... step = null, stability = null, difficulty = null, lastReview = null ...)` for freshly synced cards — server-imported cards start New per D-02 spirit); ReviewLogDto upload: populate from available entity fields (rating, reviewDatetime→ISO) and set the removed telemetry fields (elapsed_days, scheduled_days, stability, difficulty, state) to 0/"" placeholders with an explicit `// TODO(SYNC-02, Phase 4): server contract for v3 log shape` comment; the dedupe check `existingLogs.any { it.timestamp == logTime }` becomes a reviewDatetime-millis comparison (parse the DTO ISO string to millis). Legacy enum references switch to v6 imports. Document the placeholder downgrade in the SUMMARY's Next Steps for Phase 4.
  </action>
  <verify>
    <automated>./gradlew :domain:compileKotlin :data:compileDebugKotlin :data:detekt :domain:detekt</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :domain:compileKotlin :data:compileDebugKotlin :data:detekt :domain:detekt` exits 0 (app module may still be broken — Task 2 fixes it; do not run app tasks in this task)
    - `VocabDatabase.kt` contains `version = 8`
    - `FsrsCardEntity.kt` declares `step: Int?`, `stability: Double?`, `difficulty: Double?`, `lastReview: Long?`, and does NOT declare an `interval` field
    - `ReviewLogEntity.kt` declares `rating: Int`, `reviewDatetime: Long`, `reviewDuration: Long?` and retains the ForeignKey + Index on cardId
    - `Converters.kt` no longer contains LocalDateTime/Rating/State converter methods but still converts NodeType/QuestionType
    - `ReviewRepository.kt` declares `suspend fun recordReview(card: Card, log: ReviewLog)` and `ReviewRepositoryImpl.kt` contains `database.withTransaction`
    - `BackupModels.kt` contains `version: Int = 3` and `BackupRepositoryImpl.kt` contains the `backup.version < 3` rejection
    - All four domain files and all data impls import from `com.nhimz.vocabmaster.domain.fsrs.v6.` (grep count of `domain.fsrs.v6` across the listed files >= 12)
    - Seed construction in VocabularyRepositoryImpl passes `stability = null, difficulty = null, step = 0, lastReview = null`
  </acceptance_criteria>
  <done>domain+data compile green on v6 models with schema v8, atomic recordReview, backup v3, and sync compile-adapted.</done>
</task>

<task type="auto">
  <name>Task 2: App integration — QuizViewModel on v6.Scheduler + recordReview, millis consistency, field-access fixes</name>
  <files>app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/QuizViewModel.kt, app/src/main/java/com/nhimz/vocabmaster/notification/NotificationReceiver.kt, app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/MainViewModel.kt, app/src/main/java/com/nhimz/vocabmaster/ui/screens/debug_components/DataIntegrityTests.kt, app/src/main/java/com/nhimz/vocabmaster/ui/screens/statistics_components/MistakeBankTab.kt</files>
  <read_first>
    - app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/QuizViewModel.kt (scheduling block ~lines 537-551; timestamp at ~239; `.card.id` at ~550 and ~561)
    - domain/src/main/java/com/nhimz/vocabmaster/domain/fsrs/v6/Scheduler.kt (reviewCard signature)
    - data/src/main/java/com/nhimz/vocabmaster/data/database/entity/FsrsCardEntity.kt (new nullable/millis fields — DataIntegrityTests reads entities directly)
    - app/src/main/java/com/nhimz/vocabmaster/ui/screens/debug_components/DataIntegrityTests.kt
  </read_first>
  <action>
    1. `QuizViewModel.kt` — replace the legacy scheduling block (currently: construct legacy FSRS with requestRetention, call `fsrs.schedule(card, rating, now)`, then separate `vocabularyRepository.updateCard(...)` and `reviewRepository.insertReviewLog(...)` calls) with: construct `com.nhimz.vocabmaster.domain.fsrs.v6.Scheduler(desiredRetention = settingsRepository.desiredRetention.first())` (all other constructor args defaulted — DEFAULT_PARAMETERS, steps, max interval, fuzzing off), call `reviewCard(cardItem.card, rating, System.currentTimeMillis(), reviewDuration = responseTimeMs)` (responseTimeMs is already computed earlier in this function), then persist atomically via `reviewRepository.recordReview(updatedCard, log)`. Remove the legacy `FSRS`/`Card`/`Rating` imports → v6 imports (`Card` comes via QuestionWithCard; `Rating` from v6). Update the two `.card.id` accesses (review persist site and the incorrectCardIds site) to `.card.cardId` (v6 field name). Update the due-cards timestamp at ~line 239: `val nowSec = System.currentTimeMillis() / 1000` → `val now = System.currentTimeMillis()` and pass `now` to getDueCardsScoped/getDueCards.
    2. `NotificationReceiver.kt` (~line 37): pass `System.currentTimeMillis()` (no `/ 1000`) to getDueCards.
    3. `MainViewModel.kt` (~line 181): pass `System.currentTimeMillis()` (no `/ 1000`) to getDueCardCountByUnit. (Line ~272 getDueCount already passes millis — now actually consistent.)
    4. `DataIntegrityTests.kt` (reads FsrsCardEntity directly): update anomaly checks to the v8 shape — stability check skips nulls and flags `< 0.001` (STABILITY_MIN floor, message updated); difficulty check skips nulls and flags outside [1.0, 10.0] for state != New; DELETE the interval-anomaly block (column removed) and replace with a due-ordering check: `lastReview != null && card.due < card.lastReview` flags an anomaly; reps/lastReview check becomes `card.reps > 0 && card.lastReview == null`. Keep legacy `domain.fsrs.State` import working — switch to v6 State import (same values) to reduce Plan-06 churn. The importBackup assertion stays Boolean-based (Plan 07 changes the signature).
    5. `MistakeBankTab.kt` (line ~97): `it.card.id` → `it.card.cardId`. (reps/lapses usages here and in StatisticsViewModel keep compiling — v6 Card carries them.)
    Do NOT delete legacy FSRS.kt/Models.kt/FSRSTest.kt in this plan (Plan 06). Do NOT touch QuizScreen/DebugPanelScreen/OverviewTab (they still compile against legacy Models.kt enums — Plan 06 migrates them).
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin :app:detekt && ./gradlew :domain:test :data:testDebugUnitTest</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:compileDebugKotlin :app:detekt` exits 0
    - `QuizViewModel.kt` imports `com.nhimz.vocabmaster.domain.fsrs.v6.Scheduler` and contains `reviewCard(` and `recordReview(`; it no longer constructs the legacy scheduler class
    - `grep -c "FSRS(" app/src/main/java/com/nhimz/vocabmaster/ui/viewmodel/QuizViewModel.kt` returns 0
    - `QuizViewModel.kt` contains `reviewDuration = responseTimeMs` (or passes responseTimeMs as the 4th positional arg)
    - `NotificationReceiver.kt` and `MainViewModel.kt` contain no `System.currentTimeMillis() / 1000` expression; QuizViewModel's due-cards path uses millis
    - `DataIntegrityTests.kt` compiles with null-safe stability/difficulty checks and has no `card.interval` reference
    - `MistakeBankTab.kt` contains `.card.cardId`
    - `./gradlew :domain:test :data:testDebugUnitTest` exits 0 (golden, parity, optimizer, smoke suites all still green)
  </acceptance_criteria>
  <done>App builds green: production scheduling runs v6 math end-to-end, persisted atomically, with consistent epoch-millis due semantics.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
(Plan 05 artifacts)
- `ReviewRepository.recordReview(card: Card, log: ReviewLog)` (new interface method + impl using db.withTransaction)
- FsrsCardEntity v8 fields: `step: Int?`, `stability: Double?`, `difficulty: Double?`, `lastReview: Long?` (millis); interval field removed
- ReviewLogEntity v8 fields: `rating: Int`, `reviewDatetime: Long`, `reviewDuration: Long?`
- `BackupModels` v3 DTOs: FsrsCardBackup/ReviewLogBackup new shapes; `AppBackup.version = 3`
- VocabDatabase version 8 (destructive migration via existing fallback)
- DataIntegrityTests updated anomaly semantics (STABILITY_MIN floor, due-ordering check replaces interval check)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → repository | QuizViewModel scheduling writes are the app's core data mutation |
| DB v7 → v8 | Destructive migration boundary — user scheduling data is intentionally wiped |
| SyncManager → backend DTO | Placeholder telemetry fields cross to the server until Phase 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Tampering | Card update and review-log insert interleave with a crash → desynchronized scheduler state | high | mitigate | recordReview wraps both writes in db.withTransaction (D-21); Plan 06 adds a rollback test proving atomicity |
| T-01-12 | Information Disclosure | Destructive v8 migration destroys existing scheduling data | medium | accept | Explicit user decision D-02 (pre-launch, no production users); documented in VocabDatabase KDoc; curriculum auto re-seeds |
| T-01-13 | Tampering | SyncManager placeholder telemetry (0/"") downgrades server-side FSRS history | medium | accept | Phase 4 owns the sync contract (SYNC-02); placeholders are explicit TODO(SYNC-02) comments and recorded in SUMMARY Next Steps; local destructive reset already zeroes this data class |
| T-01-14 | Denial of Service | Seconds/millis unit mismatch silently breaks due-card queries | medium | mitigate | All three call sites converted to millis in Task 2; acceptance greps forbid the `/ 1000` pattern; DataIntegrityTests gains a due-ordering anomaly check |
</threat_model>

<verification>
- `./gradlew :app:compileDebugKotlin :app:detekt :domain:detekt :data:detekt` — green
- `./gradlew :domain:test :data:testDebugUnitTest` — all suites green post-cutover
- `grep -rn "domain.fsrs.FSRS\b" app/src/main/java data/src/main/java --include="*.kt"` — empty (legacy scheduler has zero production references)
</verification>

<success_criteria>
- Production scheduling uses v6.Scheduler exclusively; legacy class unreachable from production code
- v8 schema + atomic recordReview + backup v3 in place; app compiles and all tests pass
</success_criteria>

<output>
Create `.planning/phases/01-security-database-stabilization/01-05-SUMMARY.md` when done. MUST include: the recordReview atomic pattern, the seconds→millis call-site list that was fixed, and a Next Steps note for Phase 4 about the SyncManager placeholder telemetry fields.
</output>
