---
phase: 01-security-database-stabilization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/xml/data_extraction_rules.xml
  - app/src/main/res/xml/backup_rules.xml
  - app/src/main/AndroidManifest.xml
  - data/build.gradle.kts
  - data/src/test/java/com/nhimz/vocabmaster/data/database/VocabDatabaseSmokeTest.kt
autonomous: true
requirements:
  - PERS-01
  - PERS-02
must_haves:
  truths:
    - "The app's Room database (vocab_database, plus -wal/-shm), all shared preferences, and Preferences DataStore files are excluded from cloud backup AND device transfer on Android 12+ (dataExtractionRules) and from full backup on Android <= 11 (fullBackupContent) (PERS-01)"
    - "Both exclusion XML files are actually referenced from AndroidManifest.xml (currently dataExtractionRules is not referenced at all — the existing file is dead configuration)"
    - "Every VocabDao function is suspend or Flow-returning, and the production database builder has no main-thread allowance, so Room's built-in guard throws on any violation (PERS-02, D-23)"
    - "The data module can run local JVM Room tests (Robolectric + in-memory builder + runTest) (D-22)"
  artifacts:
    - app/src/main/res/xml/data_extraction_rules.xml (filled in, TODO removed)
    - app/src/main/res/xml/backup_rules.xml (new, legacy full-backup rules)
    - app/src/main/AndroidManifest.xml (both rule files referenced)
    - data/build.gradle.kts (test dependencies)
    - data/src/test/java/com/nhimz/vocabmaster/data/database/VocabDatabaseSmokeTest.kt
  key_links:
    - "AndroidManifest <application> → android:dataExtractionRules=@xml/data_extraction_rules + android:fullBackupContent=@xml/backup_rules"
    - "Exclusion paths reference the REAL database file name 'vocab_database' (from DataModule.provideVocabDatabase — NOT the unused DATABASE_NAME constant 'vocab_master_db')"
    - "VocabDatabaseSmokeTest proves the in-memory test harness Plan 06 will use for DAO/transaction tests"
---

<objective>
Secure local backups (PERS-01) and enforce the Room threading contract (PERS-02), plus stand up the data-module test harness used by later plans.

Purpose: The Room DB and preference files currently flow into Google Drive backups by default (the existing `data_extraction_rules.xml` is an unreferenced template with a TODO). Separately, PERS-02 must be *proven*: the DAO surface is already suspend/Flow, but nothing guards against regression, and the data module has zero test dependencies.
Output: Wired backup-exclusion rules on both modern and legacy Android backup paths; a documented threading audit; a green in-memory Room smoke test.
</objective>

<execution_context>
@/data/data/com.termux/files/home/.config/opencode/gsd-core/workflows/execute-plan.md
@/data/data/com.termux/files/home/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-database-stabilization/01-CONTEXT.md
@.planning/phases/01-security-database-stabilization/01-RESEARCH.md
@app/src/main/AndroidManifest.xml
@app/src/main/res/xml/data_extraction_rules.xml
@data/src/main/java/com/nhimz/vocabmaster/data/di/DataModule.kt
@data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDao.kt
@data/build.gradle.kts
@gradle/libs.versions.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backup exclusion rules for cloud-backup, device-transfer, and legacy full backup (PERS-01)</name>
  <files>app/src/main/res/xml/data_extraction_rules.xml, app/src/main/res/xml/backup_rules.xml, app/src/main/AndroidManifest.xml</files>
  <read_first>
    - app/src/main/AndroidManifest.xml (note: allowBackup="true" is present but there is NO dataExtractionRules attribute — the existing XML is dead config)
    - app/src/main/res/xml/data_extraction_rules.xml (the TODO template being replaced)
    - data/src/main/java/com/nhimz/vocabmaster/data/di/DataModule.kt (real DB file name is the literal "vocab_database" passed to Room.databaseBuilder)
    - data/src/main/java/com/nhimz/vocabmaster/data/repository/SettingsRepositoryImpl.kt (Preferences DataStore — stored under files/datastore/)
  </read_first>
  <action>
    1. Replace the contents of `app/src/main/res/xml/data_extraction_rules.xml` with real rules (remove the TODO template). In BOTH `<cloud-backup>` and `<device-transfer>` blocks add: `<exclude domain="database" path="vocab_database"/>`, `<exclude domain="database" path="vocab_database-wal"/>`, `<exclude domain="database" path="vocab_database-shm"/>`, `<exclude domain="sharedpref" path="."/>`, and `<exclude domain="file" path="datastore"/>` (Preferences DataStore lives in files/datastore/ — D-series discretion item "exclude Room DB + Datastore keys"; the DB name is `vocab_database`, the actual builder argument in DataModule.kt, NOT the unused `VocabDatabase.DATABASE_NAME` constant).
    2. Create `app/src/main/res/xml/backup_rules.xml` (legacy `full-backup-content` schema, honored on API < 31 where dataExtractionRules is ignored): the same five exclusions using `<exclude domain="database" path="vocab_database"/>` etc. (`sharedpref` path "." and `file` path "datastore" are valid domains here too).
    3. In `app/src/main/AndroidManifest.xml`, on the existing `<application>` element (which already has `android:allowBackup="true"`), add `android:dataExtractionRules="@xml/data_extraction_rules"` and `android:fullBackupContent="@xml/backup_rules"`. Keep allowBackup="true" (non-sensitive files may still back up).
  </action>
  <verify>
    <automated>./gradlew :app:processDebugMainManifest && grep -c 'dataExtractionRules\|fullBackupContent' app/build/intermediates/merged_manifest/debug/processDebugMainManifest/AndroidManifest.xml</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/main/res/xml/data_extraction_rules.xml` contains `<cloud-backup>` and `<device-transfer>` blocks, each with excludes for database paths `vocab_database`, `vocab_database-wal`, `vocab_database-shm`, sharedpref path `.`, and file path `datastore`
    - `app/src/main/res/xml/backup_rules.xml` exists with the same five exclusions in full-backup-content format
    - `app/src/main/AndroidManifest.xml` source contains `android:dataExtractionRules="@xml/data_extraction_rules"` and `android:fullBackupContent="@xml/backup_rules"`
    - `./gradlew :app:processDebugMainManifest` exits 0 and the merged manifest contains both attributes
    - The literal `vocab_master_db` does NOT appear in either XML file (wrong DB name — guard against copy-paste from the unused constant)
  </acceptance_criteria>
  <done>PERS-01: Room DB + WAL/SHM + shared prefs + DataStore files excluded on both modern and legacy backup paths; rules wired into the manifest.</done>
</task>

<task type="auto">
  <name>Task 2: PERS-02 threading audit + production-builder guard verification (D-20, D-23)</name>
  <files>data/src/main/java/com/nhimz/vocabmaster/data/di/DataModule.kt</files>
  <read_first>
    - data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDao.kt (entire DAO surface — 268 lines)
    - data/src/main/java/com/nhimz/vocabmaster/data/di/DataModule.kt (production Room.databaseBuilder lives here)
    - data/src/main/java/com/nhimz/vocabmaster/data/repository/VocabularyRepositoryImpl.kt (dispatcher usage pattern)
    - data/src/main/java/com/nhimz/vocabmaster/data/repository/ReviewRepositoryImpl.kt
    - data/src/main/java/com/nhimz/vocabmaster/data/repository/SettingsRepositoryImpl.kt
    - data/src/main/java/com/nhimz/vocabmaster/data/repository/BackupRepositoryImpl.kt
  </read_first>
  <action>
    Perform the D-20 audit and lock in the D-23 guard. This is deliberately audit-and-document work: planning has already read the DAO surface and found it fully suspend/Flow, so the code change is a guard comment, and the proof is in the acceptance greps.
    1. Audit `VocabDao.kt`: classify every function as suspend (one-shot read/write) or Flow-returning (observable read). Any function that is neither is a PERS-02 violation — fix it by adding `suspend` (one-shot) or wrapping in Flow (observable), then note the fix in the SUMMARY. (Planning found zero violations; if the executor finds one, fix it.)
    2. Audit the four repository impls: DAO calls happen inside `withContext(Dispatchers.IO)`, `flowOn(Dispatchers.IO)`, or Flow pipelines — never raw on the calling thread. Note the classification table in the SUMMARY.
    3. In `DataModule.kt`, verify the production `Room.databaseBuilder(...)` chain does NOT invoke Room's main-thread-allowance API (planning verified it does not — only `fallbackToDestructiveMigration(dropAllTables = true)` is called). Add a KDoc on `provideVocabDatabase` stating: the builder deliberately omits main-thread query permission so Room's built-in guard throws IllegalStateException on any non-suspend/non-Flow DAO call, enforcing PERS-02 at runtime; the allowance is permitted ONLY in test in-memory builders. Phrase the KDoc without naming the API method itself (keeps the acceptance grep below comment-free).
  </action>
  <verify>
    <automated>./gradlew :data:compileDebugKotlin :data:detekt</automated>
  </verify>
  <acceptance_criteria>
    - In VocabDao.kt, the count of lines containing `fun ` equals the count of function declarations that are `suspend fun` or return `Flow<...>` (the verify command proves this numerically)
    - `grep -c 'allowMainThreadQueries' data/src/main/java/com/nhimz/vocabmaster/data/di/DataModule.kt` returns 0 (and the new KDoc does not contain that literal)
    - `DataModule.kt` contains a KDoc on provideVocabDatabase documenting the deliberate omission and the test-only exception
    - `./gradlew :data:compileDebugKotlin` exits 0
    - SUMMARY will contain the DAO/repository audit classification table (tracked in output section)
  </acceptance_criteria>
  <done>PERS-02 enforced and locked: full suspend/Flow DAO surface proven numerically; Room's main-thread guard active in production with the policy documented.</done>
</task>

<task type="auto">
  <name>Task 3: Data-module test harness (Robolectric + in-memory Room + runTest) and smoke test (D-22)</name>
  <files>data/build.gradle.kts, data/src/test/java/com/nhimz/vocabmaster/data/database/VocabDatabaseSmokeTest.kt</files>
  <read_first>
    - data/build.gradle.kts (currently has ZERO testImplementation deps)
    - gradle/libs.versions.toml (available catalog entries: junit, kotlinx-coroutines-test, androidx-test-core, androidx-test-ext-junit)
    - data/src/main/java/com/nhimz/vocabmaster/data/database/VocabDatabase.kt
    - data/src/main/java/com/nhimz/vocabmaster/data/database/entity/SectionEntity.kt
  </read_first>
  <action>
    1. In `data/build.gradle.kts`, add to the first `dependencies` block: `testImplementation(libs.junit)`, `testImplementation(libs.kotlinx.coroutines.test)`, `testImplementation(libs.androidx.test.core)`, `testImplementation(libs.androidx.test.ext.junit)`, and `testImplementation("org.robolectric:robolectric:4.15.1")` (Robolectric is not in the version catalog — declare inline; if 4.15.1 fails to resolve, use the newest 4.x available and note it in the SUMMARY). Inside the `android { }` block add `testOptions { unitTests.isIncludeAndroidResources = true }`.
    2. Create `data/src/test/java/com/nhimz/vocabmaster/data/database/VocabDatabaseSmokeTest.kt`: `@RunWith(RobolectricTestRunner::class)`; build the DB with `Room.inMemoryDatabaseBuilder(ApplicationProvider.getApplicationContext(), VocabDatabase::class.java).allowMainThreadQueries().build()` (the main-thread allowance is the test-only exception documented in Task 2 — it MAY appear in this test file); in a `runTest` body insert one `SectionEntity` via `vocabDao.insertAllSections(listOf(...))` and assert `vocabDao.getAllSections().first()` contains it; close the DB in @After. Use ONLY SectionEntity (stable schema — FsrsCardEntity/ReviewLogEntity change shape in Plan 05 and this smoke test must not churn).
  </action>
  <verify>
    <automated>./gradlew :data:testDebugUnitTest --tests "com.nhimz.vocabmaster.data.database.VocabDatabaseSmokeTest"</automated>
  </verify>
  <acceptance_criteria>
    - `data/build.gradle.kts` contains testImplementation entries for junit, kotlinx-coroutines-test, androidx-test-core, androidx-test-ext-junit, and robolectric, plus `unitTests.isIncludeAndroidResources = true`
    - `VocabDatabaseSmokeTest.kt` exists, uses RobolectricTestRunner + inMemoryDatabaseBuilder + runTest, and touches only SectionEntity
    - `./gradlew :data:testDebugUnitTest --tests "com.nhimz.vocabmaster.data.database.VocabDatabaseSmokeTest"` exits 0
  </acceptance_criteria>
  <done>Data module can run real-SQLite JVM tests; harness ready for Plan 06 DAO/transaction tests.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
(Plan 04 artifacts)
- `app/src/main/res/xml/backup_rules.xml` (new resource; legacy full-backup exclusions)
- `VocabDatabaseSmokeTest` (test class in com.nhimz.vocabmaster.data.database)
- AndroidManifest `<application>` attributes: `android:dataExtractionRules`, `android:fullBackupContent`
- data/build.gradle.kts test dependencies + `testOptions.unitTests.isIncludeAndroidResources`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Android Backup Transport (Google Drive / device transfer) | Unencrypted app data leaves the device trust zone |
| Maven Central → build | New test dependency (Robolectric) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Information Disclosure | Room DB (cards, review logs) + Datastore settings leak via default cloud backup | high | mitigate | Task 1: explicit excludes for database/-wal/-shm, sharedpref root, and datastore dir in BOTH cloud-backup and device-transfer, plus legacy fullBackupContent path; merged-manifest grep proves wiring |
| T-01-09 | Information Disclosure | Backup exclusions reference wrong DB file name → silent non-exclusion | medium | mitigate | Acceptance greps pin the real name `vocab_database` (verified in DataModule.kt) and forbid the unused constant's value in the XML files |
| T-01-10 | Denial of Service | A future non-suspend DAO write blocks UI thread / crashes | medium | mitigate | Room main-thread guard active (D-23 verified) + numeric audit gate in Task 2 fails the build if any DAO function loses suspend/Flow |
| T-01-SC | Tampering | Robolectric test dependency (supply chain) | high | mitigate | Well-known Google-maintained test framework; pinned exact version from Maven Central; testImplementation scope only — never shipped in the APK; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `./gradlew :app:processDebugMainManifest` — green; merged manifest contains both rule attributes
- `./gradlew :data:testDebugUnitTest` — smoke test green
- VocabDao numeric audit — function count == suspend/Flow count
</verification>

<success_criteria>
- PERS-01 exclusions active on modern + legacy backup paths, verified via merged manifest
- PERS-02 guard proven; DAO surface 100% suspend/Flow
- Data-module JVM Room tests execute
</success_criteria>

<output>
Create `.planning/phases/01-security-database-stabilization/01-04-SUMMARY.md` when done. MUST include the DAO/repository audit classification table (every VocabDao function → suspend or Flow; every repository → dispatcher pattern used) and the final Robolectric version used.
</output>
